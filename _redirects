# Cloudflare Pages Redirects
# This file is intentionally left without wildcard rules to prevent SPA fallback.
# Existing files will be served automatically.
# Non-existent paths will result in a 404 status.
#
# NOTE: Pages Functions run BEFORE this file, and functions/_middleware.js now
# mirrors every path rule below in its RETIRED map so that query-string and
# .html variants resolve in a single hop. These rules therefore act as the
# fallback if a Functions deploy ever fails. Keep the two lists in sync when
# adding or removing a retired path.
#
# Only RETIRED paths need mirroring here. The middleware's other two
# normalizations — collapsing duplicate slashes and stripping `.html` — are
# behaviours Pages already provides natively (it resolves `//` and 308s
# `X.html` → `X`), so if Functions ever fail to deploy those URLs still
# resolve; they just cost a 308 instead of the 301 the middleware returns.

# www -> apex (requires DNS CNAME for www to be configured in Cloudflare).
# Mirrored in functions/_middleware.js (step 0) so a www URL that also needs
# a path/query fix folds into ONE 301 — this rule runs after Functions, so on
# its own it made such URLs cost two hops. Kept as the fallback, same as the
# RETIRED paths below.
https://www.simplememofast.com/*  https://simplememofast.com/:splat  301

/privacy-policy  /privacy  301
/privacy-policy/  /privacy  301
/vs/whatsapp/  /vs/  301
/vs/telegram/  /vs/  301
/vs/trello/  /vs/  301
/vs/slack-self-dm/  /vs/  301

# Cannibalization fix: consolidate /blog/captio-alternatives-comparison into /captio-alternative/
/blog/captio-alternatives-comparison       /captio-alternative/  301
/blog/captio-alternatives-comparison.html  /captio-alternative/  301

# Cannibalization fix: consolidate /blog/memo-shuukan-tips into /blog/memo-habit
# (GSC reported memo-shuukan-tips as 'alternate page' — Google deduped against memo-habit
# which covers the same 'メモ習慣化' topic with a more comprehensive 21-day program.)
/blog/memo-shuukan-tips       /blog/memo-habit  301
/blog/memo-shuukan-tips.html  /blog/memo-habit  301

# Cannibalization fix: consolidate /blog/line-keep-migration into
# /blog/line-keep-alternative. Four pages targeted 「LINE Keep」; the alternative
# page holds the cluster (45 clicks / 5,291 impressions in the 2026-08-09 GSC
# pull) while the migration guide had zero. Its unique data-export procedure was
# merged into #line-keep-export on the destination first, so nothing is lost.
/blog/line-keep-migration       /blog/line-keep-alternative  301
/blog/line-keep-migration.html  /blog/line-keep-alternative  301

# Cannibalization fix: retire the thin /vs/mem/ comparison to the /vs/ hub.
# Mem is effectively defunct and the page earned zero impressions; same
# treatment as /vs/whatsapp/, /vs/telegram/, /vs/trello/, /vs/slack-self-dm/.
/vs/mem/            /vs/  301
/vs/mem             /vs/  301
/vs/mem/index.html  /vs/  301

# Cannibalization fix: consolidate /blog/memo-app-free-guide into
# /blog/free-memo-apps-ranking. Both targeted the same 「無料メモアプリ」 cluster
# for 2026; the ranking page holds the impressions (4,027 / 33 clicks / pos 9.2
# in the 2026-08-09 GSC pull) while the guide had zero and sat in
# "Crawled — currently not indexed".
/blog/memo-app-free-guide       /blog/free-memo-apps-ranking  301
/blog/memo-app-free-guide.html  /blog/free-memo-apps-ranking  301

# Cannibalization fix: consolidate the /devlog/captio-alternative post into the
# /captio-alternative/ landing page. The devlog duplicated the landing page's
# own slug inside an already crowded Captio cluster (/captio/,
# /captio-alternative/, /blog/captio-discontinued, /en/captio-alternative/,
# /en/blog/captio-shutdown-alternatives) and earned zero impressions.
/devlog/captio-alternative       /captio-alternative/  301
/devlog/captio-alternative.html  /captio-alternative/  301

# Integrity: retired /en/blog/why-captio-died (title/content asserted an
# unconfirmed shutdown cause). Redirect to the honest Captio alternative page.
/en/blog/why-captio-died       /en/captio-alternative/  301
/en/blog/why-captio-died.html  /en/captio-alternative/  301

# Backlink hygiene: an external link (featureupvote.com, DR72) includes a stray
# closing parenthesis in the URL and lands on a 404. Reclaim it with a 301.
/)    /  301
/%29  /  301

# BEGIN data-publication (scripts/check-publication.mjs --write)
# **静的ホスティングは既定で全部配信する。**配信しないと決めたものは
# ここで明示的に 404 にする。一覧は data/publication-policy.json が正。
# **これは非公開化ではない。**リポジトリ自体が公開なので、同じ内容は
# GitHub 上で読める（publication-policy.json の repository_is_public）。
/data/audit-charter.json    /404.html    404
/data/audit-findings.json    /404.html    404
/data/autonomy-timeline.json    /404.html    404
/data/autopilot-actions-report.json    /404.html    404
/data/autopilot-actions.json    /404.html    404
/data/benchmark.json    /404.html    404
/data/content-graph.json    /404.html    404
/data/corporate-obligations.json    /404.html    404
/data/cpp-map.json    /404.html    404
/data/credential-expiry.json    /404.html    404
/data/crossrepo-probes.json    /404.html    404
/data/emergency-stop.json    /404.html    404
/data/escalation-rules.json    /404.html    404
/data/feature-backlog.json    /404.html    404
/data/feature-outcomes.json    /404.html    404
/data/financial-policy.json    /404.html    404
/data/injection-surface.json    /404.html    404
/data/kpi-definitions.json    /404.html    404
/data/model-eval-set.json    /404.html    404
/data/model-routing.json    /404.html    404
/data/monitoring-coverage.json    /404.html    404
/data/pr-claims.json    /404.html    404
/data/publication-policy.json    /404.html    404
/data/revenue-series.json    /404.html    404
/data/review-intake.json    /404.html    404
/data/review-replies.json    /404.html    404
/data/signal-ledger.json    /404.html    404
/data/site-constants.json    /404.html    404
/data/spend-approvals.json    /404.html    404
/data/stop-drills.json    /404.html    404
/data/vendor-register.json    /404.html    404
# END data-publication
