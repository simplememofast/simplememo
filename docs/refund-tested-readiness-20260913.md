# 返金検出：テストでの運用準備完了と本番自動監視

2026-09-13、オーナーは「テストで運用準備を完了し、本番の初回検出は自動監視に任せる。テスト完了時にスコアをあげる」と明示指定した。返金を意図的に本番で発生させたり、自社の返金方針を変更したりする指示ではない。

## この1業務の完了条件

1. 返金通知の検出・記録・重複防止・保存失敗からの回復・環境分離をテストする。
2. Apple実署名の検証と、実際の本番TEST通知の配信成功を確認する。
3. 本番の定期監視を反映し、実際のcron実行記録と読戻しを確認する。
4. 初回検出・欠測・エラー時の継続対応を既存の自動実行へ引き継ぐ。

監視はProductionのREFUNDのみで検出状態を更新し、REFUND_DECLINED・REFUND_REVERSEDは別件数として保持する。Sandbox・環境不明の記録を本番返金へ混ぜない。読取り失敗は件数null／エラーで、0件と区別する。対象は保持済み180日間の通知で、全顧客の返金不存在や返金率を証明するものではない。

## テストの証明範囲

- ローカルworkerd/D1の結合テスト：署名検証の境界だけをテスト内で置換し、HTTP処理・アプリ照合・実SQL・重複処理・保存失敗と回復・監視を通す。Production/Sandboxのラベルはローカルfixtureである。
- 実Apple JWS検証：Appleが署名したTEST通知と固定したApple公開ルートで、正常系と改ざん等の拒否を確認する。
- 本番TEST疎通：Appleが現在の本番Webhookへ送信し、配信結果SUCCESSを確認する。TESTをREFUNDの実検出へ読み替えない。
- Get Refund History：既存のページング・件数上限・不完全取得・暗号化テストを再実行する。別記の実履歴観測は、更新取引24件を出所とした照会で返金0件だった。

Apple Sandboxで購入から返金まで行った実績や、本番の返金検出実績は今回の証拠に含めない。テストデータを本番の返金テーブルへ挿入しない。実課金・返金申請・TestFlight配信は行わない。

## 採点上の扱い

task index 146「返金の検出（Get Refund History）」のみを、上記条件の達成後に `nobody` から `ai_executes_gated` へ移す。採点条件の変更は `automation-coverage.json` の `acceptance_revisions` に残す。

| 指標 | 変更前 | 変更後 |
|---|---:|---:|
| AI実行率 | 159/176 = 90.340909% | 160/177 = 90.395480% |
| 総合自動化率 | 159/192 = 82.812500% | 160/192 = 83.333333% |
| AI関与率 | 166/176 = 94.318182% | 167/177 = 94.350282% |
| カバー率 | 176/192 = 91.666667% | 177/192 = 92.187500% |

AI実行率の増分は約0.054571ポイント。未着手の1件が対象に入るため分母も176→177になる。総数203・対象192は不変。他業務の完了条件へ一般化しない。

変更前の「本番の実返金を必須とする」基準では159/176のまま。新基準の160件のうち1件はテストでの準備完了と稼働中の監視に基づく。本番で初めて返金を検出した時は実績欄を更新し、加点は繰り返さない。月次系列の9月増加件数もこの1件を含むことを明記する。

## 実行証拠

機械可読の実行・配信・本番読戻し結果は [refund-readiness-20260913.json](../data/refund-readiness-20260913.json) に保持する。認証情報、取引ID、生の通知、口座情報は公開しない。
