# R4 / R5 の取得待ちを再確認した結果 — 2026-09-15

既存SEO DailyのBigQuery出力から、2026-08-15〜2026-09-11の28日間のクエリ×ページを読み込めた。既存Company readerの原本・取得元・期間・品質検証を通し、追加クエリ、CSVの手動取得、鍵の作成は行っていない。古い「オーナーの追加エクスポート待ち」は、現在の取得状況を表していない。

## 今回の判断

- **R4：現状のページを維持する。** 旧キューが着地不明としていた名前付きクエリは、今回の取得範囲で着地を確認できた。ただし別の期間の観測であり、旧表の数値を復元したものではない。現在のページ別実績、本文の役割、既存のノイズフロアと進行中実験を照合し、統合・転送・C19新設を正当化する根拠は揃っていないと判断した。データ取得を人へ再依頼せず、既存ページを維持する。
- **R5：音声4LPを維持し、統合しない。** クエリ×ページの取得経路は動いているが、4面すべての検索語を網羅できてはいない。原収集には表示回数の下限があり、匿名化クエリも含まれない。行が無いことを表示ゼロや競合ゼロに変換しない。各ページの役割と現在の本文を確認したが、この範囲だけで統廃合を決めない。
- **C19：blockedを維持する。** 理由を古い手動エクスポート待ちから、既存ページとの役割・独自価値・iOS実機検証と元の公開ゲートへ更新する。データが読めたことは、新規ページの許可ではない。

6つの関係ページについて、本番の内容が確認時点のmainと一致することも照合した。ページ、canonical、転送、計測、既存実験の開始日・評価日・分母には変更を加えていない。この判断は事業成果や新しい定期完走の実績ではない。

## 次の既存運転で読むもの

1. `docs/autonomy/OPERATING_RUNBOOK.md` に従い、既存収集を再利用する。Companyの `growth-status --full` で選ばれたGSC入力の取得元・期間・品質と `query_pages` を確認する。
2. クエリの着地は、同じ検証済みスナップショットの `queryPages` で確認する。ページ集計から推測したり、別期間の結合行を借りたりしない。
3. 不足があれば対象クエリ・対象面・原収集の下限・匿名化を分けて記録する。同じデータ不足を理由に、既存取得と重複する手動CSVを自動的に依頼しない。
4. 次の変更は、新しい観測と具体的な原因、既存実験との重複、独自価値、価値契約・品質・1日1施策のゲートで判断する。今回の維持判断を、将来の変更禁止や全件の恒久的な棄却として使わない。

原データ、原本SHA、今回のページ照合と具体的な判断根拠は、非公開の `~/.config/simplememo/company-os/queue-r4-r5-*.json` に保存する。元のキューの履歴値とdropped/done判断は保持する。再開には現在の非公開Goal stateと既存Runbookを使い、新しい収集・レポート・schedulerは作らない。
