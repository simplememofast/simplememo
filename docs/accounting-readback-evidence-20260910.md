# 会計読取の実証と月次締めに残る業務

2026-09-10。既存の認可を使い、法人の銀行明細、収入・支出取引、勘定科目、公式仕訳帳CSVを実際に取得した。台帳119の「残高だけ」「明細と仕訳側は未着手」という取得状況を訂正する。アプリの作り直しや公開は不要で、途中のアクセストークン期限切れは既存の通常更新処理で解消できた。

## 確認できたこと

- 銀行明細の日付・金額・口座と既存取引の支払行が対応し、公式仕訳帳に同じ取引IDがある記帳を確認した。明細から取引への直接外部キーは返っておらず、対応条件と取引IDによる仕訳帳照合を区別した。
- 別の入金明細では取引登録待ち額が全額残り、取得範囲の収入取引・仕訳帳にも対応する記帳が見つからなかった。これは登録待ちであり、未入金・未送金の判定ではない。
- 外部決済の総額・手数料・正味額と既存の記帳を照合した。正味額による既存記帳は確認できたが、総額と手数料の記録方法や税区分の正誤は未判断である。
- 仕訳帳の貸借合計は一致した。これは算術上の一致であり、全取引の網羅・経理の正しさ・月次締め完了を証明しない。

## 取得範囲と証拠の保持

収入取引は2026-01-01〜09-10、支出取引と仕訳帳は07-01〜09-10、口座振替は08-01〜09-10を指定した。APIのページ末尾まで取得したことは原本の網羅を保証しない。仕訳帳に出力された取引日は07-01〜08-06で、未登録明細は仕訳帳に含まれない。

実額・名義・口座識別子・取引ID・原本CSVと読み取り記録は非公開の証拠領域に保持する。本書にそれらの内容を転載しない。原本CSVのSHA-256は `064e797430cb78f0355a145db0f0b454b29c76bba9e9528a19b9a67e41d965c3`。独立したレビューで、記帳・登録待ち・銀行決済・取得範囲を混同していないことを確認した。CIが会計サービスを再取得するという意味ではない。

## 未完了と実行分類

会社関連の銀行原本による送金対応、書類、期首残高・未登録明細を含む対象期間全体、総額と手数料・税区分・配賦の根拠が残る。そのうえで必要な仕訳処理と月次締めの統合を実行・検証する。既存仕訳の重複登録、根拠のない税区分変更、仕訳作成や締めの先取りはしない。

台帳119は `nobody` のまま。取得を阻む理由を `external_credential` から `external_data` に訂正する。解除条件の識別子 `bank_feed` は履歴のため維持し、内容を新規連携契約から、残る資料の照合と会計業務の統合実行へ変更する。優先順位は残る資料と実行の確認待ちとする。閲覧・集計・既存仕訳の確認・通常認証更新では加点しない。月次予算115と照合済み別業務125にも重複加点しない。棚卸し、分母、本人同意、配信条件を変更しない。
