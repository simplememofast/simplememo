# 実装後に残る実行証拠と待機条件

2026-09-10。実行率は156/179＝87.150838%、棚卸し203・除外4・定義199件を維持する。
最新目標は[実測99.497%](ai-execution-999-2026-09.md)。今回の整理は加点しない。

優先順位の未評価3件を、2件の検証待ちと1件の残設計・実装調査へ更新する。コードの存在を再発見するだけの
反復を避け、次に何が確認できれば進めるかを残す。評価は24時間で期限切れとなり、
新しい実データ・配布・障害があればその時点の実体を再確認する。

## 本番改善サイクル

`data/feature-outcomes.json` の階層保存先改善は、出荷前に測り方を宣言済み。
`declared_not_shipped` のまま、28日観測、固定レビュー日2026-10-18、
既定の試行数・利用端末数が残る。実機確認・同一版の配布記録が揃ってから観測を始め、
結果を後付けしたり、旧版の実績を同一版の結果へ転用したりしない。

## Captureと購読の実測

[iOS PR440](https://github.com/simplememofast/simplememo-ios/pull/440)の保存先証拠、
[PR461](https://github.com/simplememofast/simplememo-ios/pull/461)の初回送信先と実配送先の
区別は最終CI成功後にマージ済み。これらは既存TestFlight1316の変更ではない。
ただし現行API集計の`zero_decision_rate`は常にnullで、完全な操作観測・全Captureの分母を
提供していない。`VISION.md` §14にある「正しい場所」の判定方法も未決定である。
**NSMの行はinspectを維持する。** 既存証拠を再利用しながら、測定契約・観測範囲・
集計実装の不足を具体化する必要があり、配布待ちだけでは完了しない。
そのうえで同一版の実機・本番収集・削除・集計を結んだ観測が必要であり、購読通知単独、
Simulator結果、配布前コード、空の集計は実測の完了証拠ではない。

## アプリ内のサービス案内

[API PR303](https://github.com/simplememofast/simplememo-api/pull/303)は
Worker1.5.63へ反映済み。`GET /v1/service-notice` の実応答は200で`notice:null`、
キャッシュ60秒。公開文面の読み取り経路で、新しい書き込みAPIやメール送信はない。
[iOS PR462](https://github.com/simplememofast/simplememo-ios/pull/462)は最終CI成功後にマージし、
マージ後の検査も成功した。表示・期限・撤回・オフライン時の扱いを検証済みだが、
このコードを含む版の配布と、実際に必要な案内の到達確認は残る。

架空の障害を発表して配信実績を作らず、空の案内を全利用者への到達と数えない。
同意済みメールの既存範囲を拡張したり、利用者本人の同意を代行したりしない。

## 配布の扱い

ユーザー選択Cに従い、必要な改善をまとめてiPhone13で同じ版を確認する。
必要な配布を単独の小変更ごとに作らず、1316の実機作業と次版の実装を混同しない。
PR TIMESは検証済み保存155/178から3ポイント以上改善した時、または最終目標到達時に同期する。
