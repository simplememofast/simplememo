# AI実行率90%超への実行計画

2026-09-07更新。オーナーの依頼は「AI実行率90%超えを目指して新たな施策機能も含めてデプロイまで」。既存の最大権限の委譲を引き継ぐ。これは達成記録ではない。

## 分母と判定

現在の変更案はAI実行144 / 実施中176 = 81.818…%。総合自動化率144 / 199 = 72.4%、AI関与率158 / 176 = 89.8%、カバー率176 / 199 = 88.4%。単純化候補の登録に続き、AIが月次予算を決定・適用した1件を移管。タスクの新設や細分化はしていない。本番配信確認前は公開済み143/176=81.3%と区別する。

AIが既存の実施中業務を k 件引き受け、未着手から n 件を開始すると、AI実行率は `(144+k+n)/(176+n)`。90%ちょうどではなく、厳密に90%を超えることを要件とする。

- n=0なら k=15 が必要。159/176 = 90.3%。
- n=10なら k=14 が必要。168/186 = 90.3%。
- n=23なら k=13 が必要。180/199 = 90.5%。
- 未着手23件だけをすべてAI化しても167/199 = 83.9%。

再計算: `node scripts/autonomy-gap.mjs --execution-plan --target 90`。
機械可読出力: 同じコマンドに `--json` を付ける。
従来の `--plan --target` は総合自動化率用で、その動作は変えない。

## 既存業務を移す候補

現在の到達可能分類は既存実施中10件・未着手16件。全件を満たした楽観上限でも170/192 = 88.5%。包括委任を反映し、全体運営予算の停止理由を承認待ちから資料照合・実行待ちへ修正した。実装や観測が終わったという意味ではない。

既存実施中10件の候補:

1. D7/D28・課金・解約を含む評価。解約までの取得・照合が必要。
2. 製品バグの問い合わせから再現テスト生成。操作手順と実際の再現が必要。
3. App Review提出。対象コミットの実機確認と提出実行が必要。
4. 審査後のApp Store公開。審査通過と公開実行が必要。
5. 段階公開の昇格。母数・経過時間・実際の昇格が必要。
6. CAC・LTV・回収期間・粗利の統合。期間を合わせた実測が必要。
7. 契約の責任上限・知財・個人情報・準拠法の条項検査。
8. AI事業者のDPA・データ利用・SLA・撤退計画の審査。
9. 定型契約・条項比較・リスク抽出。
10. 全体の運営予算。AI費用だけの決定を二重加点せず、収支・残高・ベンダー費用を照合して配分する。

その他の意思決定・実行候補（既完了との重複を実行前に照合する）:

- 要望の設計採否: VISIONとの適合、利用者の根拠、受入条件を残して決定する。
- Remote Feature Flag: 対象フラグ・影響範囲・復旧方法を確認して実行する。
- ChatOpsリリース起動: 検証した版と実行結果を結ぶ。実機確認の捏造はしない。
- AI費用の月次上限: 2026-09-07にAIが$280維持と種別配分を決定し実行ゲートへ適用。今回⑤に1件だけ計上。
- アプリ運営の月次予算: 収支と必要費用を照合して配分し、決定を記録する。
- オンボーディング改善: 価格や既存契約を変更せず、導線変更を検証・配信する。
- プレミアム機能の設計判断: 有料範囲・既存利用者への影響を確認して設計を確定する。
- 推論の実行場所: データの扱い、端末要件、費用から決め、設定・実装と一致させる。

これらは条件付きの候補経路であり、既にAI実行に計上された設計採否などを再計上しない。実機・審査・標本数・原資料の不足を解決できた保証はない。

## 着手順と証拠

### 引き継ぎ後の実行記録（2026-09-07）

- 単純化候補と143/176の台帳はWeb PR #1065でマージ。Cloudflare Pages本番デプロイ `6de1964a-e91b-4e35-94c9-429b9742dc60` が対象SHA `aace2f044baa85061010d6a6edb6d1a90db83018` で05:47:45 UTCに成功した。9月以外の系列と配信済みPRの文言は変更していない。
- 一括事前検証がCIの7本の既存テストコマンドを対象外にしていたため、`node --test` の複数ファイルと `growth/lib/*.test.mjs` を接続。元の178本から、7本と導出器自身の検査を含む186本をローカルで実行し全件成功。後続ファイルを省く変異では実CLIの失敗検知を含む2検体が失敗した。これは出荷前QAの改善であり、「不要になったテストの整理」の完了や追加のAI実行業務には数えない。
- 本番D1の `apple_refund_events` をObserve権限で環境・通知種別ごとに集計。2026-09-07 05:45 UTC前後の照会はHTTP 200、結果0行、DB書き込み0。保存された実返金通知の観測は依然0件で、返金検知成功には加点しない。返金が発生していないことや通知経路の完全性を、この空の結果だけでは証明できない。

今回の端末作業はない。App Review・App Store公開・同一ビルド実機確認の状態は更新していない。90%超には既存の実施中業務だけを移す場合、さらに16件の実行証拠が必要。候補化・QA検証・本番集計の読み取りを製品改善効果へ流用しない。

### 残る業務

まず既存の調査を使える契約・DPAの審査を進め、AI自身の判定・根拠・未確認を人の過去判定と区別して保存する。並行ではなく、その後に予算・設計判断の実行条件と適用経路を作る。価格変更や不要な新規支出を目標達成のために発生させない。

プロダクト側では課金失敗からの回復、アプリ内障害案内、Zero-decision Capture Rateの計測を進める。これらは未着手からの開始なので、分母増加を含めて再計算する。

加点は実行結果・適用先・対象版を確認した既存タスクだけに行う。新しいタスクが業務上必要なら棚卸しに追加するが、細分化で率を上げない。合成テストは動作検証であり、実際の返金・実機確認・契約同意・利用者同意の代用にはしない。

2026-09-07時点: Obsidianの階層保存は開発ビルドで実機確認済み。課金回復導線まで含む5.8.34 (1110) のTestFlight配布が完了。配布版の実機確認とApp Review提出は残っている。CI履歴の読み取り監査はテスト整理の完了と数えない。単純化候補email_setup_primary_manualを既存の採点・ロードマップへ載せた分だけ反映し、AI実行率は81.25%。製品改善効果の達成や90%達成は宣言しない。


2026-09-07の包括委任後: `docs/monthly-budget-decision-2026-09-07.md` に実績・判断・適用先・検証を記録。spend-approvals seq=3はAIによる判断であり、人の承認に偽装していない。主系9回の実績から総額$280を維持し、記事$125・修理$115・予備$40へ配分。⑤の1件だけを反映し、同じ判断を⑥へ二重計上しない。現時点の公開確認は前節のPR #1065/#1066までで、今回の変更案の本番配信は別に確認する。目標は引き続き90%超であり、完了扱いにしない。
