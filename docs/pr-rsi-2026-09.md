# PR TIMES 原稿ドラフト — 自律運用（RSI）

> **配信前ドラフト（社内レビュー用）。配信は PR TIMES 管理画面から手動。**
> 根拠: `pr-rsi-2026-09-evidence.md`（3リポジトリの実装からの裏取り台帳）
> 段取り: `pr-rsi-2026-09-plan.md`
> 作成: 2026-08-22 ／ 配信目標: 2026年9月上旬（**平日の朝・G4**）
>
> **この原稿には、証跡の無い主張を一切入れていない。**入れなかったものと理由は
> 台帳 §10「書けない」を参照。追記したくなったら、先に台帳の判定を動かすこと。

---

## 0. 訴求の設計（この原稿の前提・配信時は削除）

- **フックは「AIがすごい」ではなく「人がいないのに毎朝出荷されている」。**
  自社実測で、AIを前面に出したPR④は469PV・D-SCORE 21で最下位、
  AIに触れなかったPR②（Obsidian連携）は12,188PVで最高だった（台帳 §8-1）。
  D-SCOREのゲート **G2 = 見出しにAI・煽り語を入れない**はこの実測から来ている。
  → **見出しはA案（AI語なし）を第一候補とする。**
- **数字は実測のみ。**Zero-decision Capture Rate、ルーティング精度（Day1 80%等）、
  広告CPI、トークン配分は**すべて実測が無いので書かない**（VISION §5.2・§10 の但し書き、
  CLAUDE.md の方針と同じ基準）。
- **一番効くのは「できないことを明記していること」。**RSIの記事で記者が最初に疑うのは
  暴走と誇張。先に自分で境界を言う原稿は、そこで信用を取る。

---

## 1. タイトル（候補）

### A案（推奨・G2準拠／AI語を見出しに出さない）

> **個人開発のiOSアプリが、12日連続で「無人のまま」機能改善を本番リリース —
> 検証を通ったコミットだけが出荷される仕組みを公開**

### B案（体制の異常性を前に出す）

> **社員0名のiOSアプリ、毎朝6時に自分で改善案を選んで出荷する運用を12日間継続。
> 「出荷の最終判断だけは人間」という境界を権限で固定**

### C案（AI語を使う／G2の解釈をオーナーが変えた場合のみ）

> **「Captio式シンプルメモ」、アプリの改善・記事執筆・出荷判断をAIが自走する
> 運用体制を公開 — 人間が握っているのは"App Storeへ出す最後のボタン"だけ**

**サブ候補（共通）:**
- 手順書1冊とチェック9本。「書く理由がない日は書かない」まで含めて自動化した
- 自動化の成果物ではなく、**自動化が止まったことを検知する仕組み**を公開します
- AIに与えなかった権限を、理由つきでソースコードに書いてある

---

## 2. リード文（要約・180字前後）

株式会社ユリカ（所在地：日本）は、iOSアプリ「**Captio式シンプルメモ**」の開発・運用において、
**サイト改善・記事制作・品質検証・出荷までを自律的に実行する運用体制**を構築し、
2026年8月11日から**12日間連続で稼働**したことを公表します。毎朝6時にAIエージェントが
検索データを読んで当日の施策を1件だけ選び、実装し、9種の自動検証を通し、
**検証に合格したコミットだけ**が本番へ反映されます。同時に「**AIに与えていない権限**」と
「**現時点でできないこと**」もソースコードと手順書に明記して公開しました。

---

## 3. 本文

### ■ 背景：自動化の失敗は「エラー」ではなく「静かな停止」で来る

個人・小規模チームの開発運用にAIを組み込む試みは増えていますが、多くは
「AIに書かせる」で止まります。実際に運用して分かったのは、**本当の失敗は
派手なエラーではなく、誰も気づかない停止だった**ということでした。

同社の成長計測基盤 `growth/` は、まさにその失敗から生まれています。手順書の
冒頭には、こう書かれています。

> 12ページのタイトルを変更し、評価日を報告書に書いた。その日付は過ぎた。
> 以後6週間の報告書はいずれも「その日に判断する」と繰り返して先へ進んだ。
> 壊れてはいなかったし、誰も不注意ではなかった —— **単に、期限切れになれる
> 成果物が無かった**。計画は文章の中に住んでいて、文章は手を挙げない。

以降、同社は方針を「AIに賢く働かせる」から「**判断の根拠と期限をファイルに落とし、
CIに監視させる**」へ切り替えました。今回公開する体制はその延長線上にあります。

### ■ 何が動いているのか：毎朝6時の1サイクル

```
06:00  検索データ（Google Search Console の BigQuery エクスポート）を読む
   ↓   その日に実装する施策を「1件だけ」選ぶ
   ↓   ・期待クリック数が基準未満の根拠では書かない（ノイズフロア）
   ↓   ・書く理由がなければ書かない。保守作業とログだけで終えてよい
   ↓
       実装 → 内部リンク配線 → コンテンツ台帳へ登録 → OG画像生成 → QRコード生成
   ↓
       9種の自動検証（SEO検証261ファイル／CSS・JSキャッシュ整合／URL正規化／
       内部リンク13,195件＋構造化データ5,203件＋サイトマップ576URLの直接200検証／
       実験台帳の期限監視／コンテンツ台帳の整合／サイトマップ掲載の強制 ほか）
   ↓
       Pull Request を作成
   ↓
       **検証に合格したときだけ**、合格したコミットを指定して自動マージ
   ↓
       本番デプロイ（Cloudflare Pages）
10:00  実行結果を集計してオーナーへ日報メール
12:00  「緑のまま何もしていない」状態を検知する別系統の見張り
```

**「毎日1本出す」ことは目標にしていません。**手順書には
「**毎日出すために基準を下げる、はこの運用の失敗定義そのもの**」と書かれており、
基準を通らなかった日はスキップし、**その理由が日報に載ります**。

### ■ 実際に出荷されたもの（2026年8月11日〜22日）

- 情報ハブ記事の新設（Obsidianの保管庫解説、プラグイン実測ランキング、
  Logseqとの比較検証 ほか）
- **一次情報の実測**：Obsidian公式レジストリから登録6,840個のプラグインを当日実カウントし、
  ダウンロード数・最終更新日・配布版とタグの不一致まで確認して記事化
- サードパーティアプリの実挙動検証（同一フォルダ共存・往復書き込み・
  DB版の非Markdown保存をスクリーンショット付きで確認）
- スマートフォン実描画QAで見つけた横スクロールの自己修正
  （CSS Gridの `min-width: auto` が原因と特定し、そのページだけを局所修正）

### ■ 「AIに与えていない権限」を、理由つきでソースコードに書いてある

同社が今回もっとも公開したいのは、できることの一覧ではなく**境界**です。

アプリのリリース経路（タグ作成 → Xcode Cloud でのビルド → TestFlight配信 →
App Review提出）は自動化されていますが、**その連鎖を起動する権限を、
開発用のAIワークフローには与えていません**。設定ファイルには理由がこう書かれています。

> この権限があれば、確認を求めない設定のAIが出荷ワークフローを直接呼び出し、
> **人間を一切介さずApp Storeへ出荷できてしまう**。この機能に必要な中核用途は無い —
> リポジトリの読み取り・コミット・push・PR作成・コメントはすべて権限なしで動く。
> 必要になったら、この権限を広げるのではなく、**用途を限定した別の経路を作れ**。

加えて、リリース手順書には**人間のハードルール**が明記されています。

> **実機で確認してから提出する。後ではなく先に。**

Appleの審査通過後にApp Storeへ公開する操作も、設定上ハードコードで無効化されており、
**恒久的に人間の操作**として残す方針です。

### ■ 品質検証：AIが「まだ誰も報告していないバグ」を見つけた

2026年7月末に導入した自律QA基盤は、「AIに毎回画面を操作させる」方式を**採っていません**。
回帰検証は決定論的なXCTest/XCUITestが担い、AIは**テスト設計・失敗の分類・
資産化**を担当します。テストからは本番APIへ到達できないよう構造的に遮断し、
成功・オフライン・タイムアウト・500・429を再現可能な形で作り分けています。

この設計中に、AIが**ユーザーからの報告が一件も無かった不具合**を発見しました。
Apple Watchからの送信が失敗して別経路へフォールバックした際、
**同じメモが2通送られうる**（経路ごとに識別子が変わるためサーバ側の重複排除も効かない）
というものです。重複排除の台帳を追加して修正し、同時に回帰テストとして固定しました。

失敗を分類するAIには、**「根拠が足りない」と答える選択肢**を仕様として持たせています
（推測で断定させないため）。また本番影響・課金・認証・データ消失に関わる場合は
**必ず人間確認**と回答するよう定めています。

### ■ セキュリティ：AIが発見し、AIが修正した

2026年7月の堅牢化では、iOSアプリ全65ファイルとリレーAPIを監査し、
**「正規の利用者から見て挙動が1ビットも変わらないこと」**を制約に4件を修正しました。
最も重い1件は、認証コード確認エンドポイントに対し並列リクエストで
試行回数の上限を実質的に超過できるというもので、**他人のメールアドレスを
勝手に確認済みにできる入口**でした。

### ■ ユーザーの声はどう機能に変わるか

Apple Watchの同期不具合を4回にわたり診断報告してくださった利用者の方から、
2026年7月31日に2件の要望をいただきました。同社はこれを、
**Obsidian連携を設定した利用者の7日後継続率が、未設定の2.5〜3.7倍**という
自社の実測データと突き合わせて優先度を決め、**4回のビルドに分けて納品**しています
（送り先ノート名の編集 → 設定画面での発見性改善 → メール送信のON/OFF →
追記テンプレート）。

うち1回は、**実装は正しく動いていたのに、設定画面から機能の存在が分からなかった**
という理由で追加のビルドを出したものです。

### ■ できていないこと（同社が手順書に明記しているもの）

自律運用の手順書には「**できないこと（正直に）**」という節があり、
以下が明記されています。

- **iOSシミュレータ・実機iPhoneの操作、撮影、計測**（macOS環境が必須のため）。
  アプリ画面が必要な記事は撮影依頼として積み、画像が入ってから公開する
- App Store Connect等、権限がオーナーにしかない操作
- ブランド判断や大きな構造変更など、判断に迷うものは実装せず起票する

さらに、自律運用が**失敗した日の記録も残しています**。定期実行の初回不発、
権限設定による成果物ゼロ、外部ドメインへの到達制限、二重起動——
いずれも原因と対処が実行ログに1件ずつ書かれています。

**同社は「完全自動化できた」とは主張していません。**主張しているのは、
「**自動化が止まったことに気づける形にした**」ことです。

### ■ 提供・関連情報

- アプリ：**Captio式シンプルメモ（Simple Memo - Captio-style）** iOS
- 価格：無料（7日間の無料トライアル付き）。無料プランは1日3通、
  Premiumプラン（月額500円／年額5,000円）で送信無制限
- App Store：https://apps.apple.com/jp/app/id6758438948
- 公式サイト：https://simplememofast.com
- 【要作成】本リリースの受け皿ページ：https://simplememofast.com/autopilot/

---

## 4. ボイラープレート（会社・アプリ概要）

**Captio式シンプルメモ（Simple Memo - Captio-style）について**
思いついた瞬間にメモして、ワンタップで自分のメールに送れるiOS向けの軽量メモアプリ。
起動 約1秒・ワンタップ送信の高速体験、オフライン対応（Outbox）、
端末内AES-GCM-256暗号化によるプライバシー保護を特長とする。
サービス終了した「Captio」の代替として2026年にリリース。
Obsidian連携・音声自動入力・Apple Watch・Siri（AirPods経由）に対応。
※エンドツーエンド暗号化ではありません（メールは標準のSMTPで届きます）。

**運営：株式会社ユリカ（YURIKA, K.K.）／日本**

---

## 5. 配信メモ（社内用・配信時は削除）

### 5-1. 意図的に書かなかったもの

| 書かなかったもの | 理由 |
|---|---|
| 「複数のAIモデルによる多重監査」 | 実装は単一モデル。「複数の**層**」に置き換えた（台帳 §3） |
| ahrefs / AppsFlyer / Firebase のデータ分析 | 自動連携なし。本文はGSC/BigQueryのみに絞った（台帳 §4-2） |
| Computer Useによる配信自動化・ヒーローバナー自動生成 | 配信は手動。OG画像生成は事実だがPR素材の話と混ざるので落とした |
| リスティング広告の自動運用・リアルタイム最適化 | 未実装。かつ自社分析が「この予算規模ではやるべきでない」と結論（台帳 §4-4） |
| SNSマーケティングの自動化 | X英語BAN・TikTok停止・scheduled経路が8/7から故障中（台帳 §4-6） |
| TestFlight実機テストのヒューマンレス | Hard Ruleで人間必須。**むしろ強みとして書き直した**（本文「境界」節） |
| トークン予算の自己最適配分（⑤） | 証跡ゼロ（台帳 §6） |
| 売上データからの月次予算自動調整（⑥） | 受け皿のみ・オーナー判断待ち（台帳 §7） |
| 法人経営の自動化（⑦・全7項目） | 3リポジトリに証跡ゼロ（台帳 §7） |

### 5-2. 画像素材

- **ヒーロー／OGP（1200×630）**：未作成。`docs/siri-hero-banner-brief.md` の様式で
  依頼文を書き起こす（画像内に文字を入れない・左半分に余白）。
  推奨モチーフ：**夜明けの空っぽのデスク／画面には06:00にPRがマージされていく様子**
  （人が居ないことが一目で伝わること。ロボットや脳のイラストは使わない — 陳腐化）
- **図解1枚（本文中）**：本文§「毎朝6時の1サイクル」の流れ図。
  **これは記事の転載時に一番使われる**ので、単体で意味が通る形にする
- 参考として出せる実物：日報メールのスクリーンショット、
  実行ログ（`AUTOPILOT_LOG.md`）の該当ページ、`actions: write` を与えていない設定ファイル

### 5-3. 禁止表現（事実誤認防止）

- **「完全自動化」「無人経営」「人間不要」と書かない。** 出荷の最終判断・実機確認・
  App Store公開は人間が持っている（これは意図的な設計であって、未完成ではない）
- **「AIが経営している」と書かない。** ⑤⑥⑦に証跡がない
- **「世界初」と書かない。** 検証不能（既存の `pr_release` 運用ルールにも同じ規定あり）
- **数値は実測のみ。** ルーティング精度・Zero-decision率・CPIなど未実測の数字を出さない
- **「エンドツーエンド暗号化」とは言わない**（従来どおり）

### 5-4. 配信条件（自社ルール）

- **D-SCORE ≥ 60**（見出し確定稿で再採点）→ 採点案は `pr-rsi-2026-09-plan.md` §2
- **G1** サムネイル1200px ／ **G2** 見出しにAI・煽り語を入れない ／
  **G3** PR TIMES配信 ／ **G4** 平日の朝 — **1つでも落ちたら配信しない**
- 配信後は `growth/experiments/experiments.json` の該当レコードを `running` にし、
  評価日を配信日+14日に設定。`growth/data/annotations.json` に配信日を追加する
