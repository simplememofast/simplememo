# 購読評価と返金の実観測（2026-09-10）

AI実行率の定義・実行者・加点は変更しない。task11の古い「統合未実装」を、現在の実装と実観測の不足へ更新する。task146は実際の返金通知待ちを維持する。個別インストールや取引情報、非公開集計の数値はこの文書へ掲載しない。

## 同一コホートの評価

API [PR #306](https://github.com/simplememofast/simplememo-api/pull/306) は、既存の日次Retention Evaluationに購読cohort readerを追加した。PRの同一head `7e1c611c8c8c54358a415cb5ea4411d391ea83c4` で4チェックが成功し、main `26be97f312f3f4b2812ae1d176b3bbd70622350d` に反映した。

既存workflow [34440775851](https://github.com/simplememofast/simplememo-api/actions/runs/34440775851) を同じmain SHAで実行し、2026-09-10 05:22:07 UTCの本番評価をartifactと非公開APIリポジトリへ保存した。実行・保存の全stepが成功している。

同じ2週・同じas_ofの4読取は成功したが、購読cohortの適格対象は両週とも空だった。旧usage対象の元の初回起動にrelease/ios_device contextが無く、既定の除外条件を満たさない。旧usageの母数を新しい集計へ流用せず、両sectionを別々に保持した。判定は `continue_measurement`、解約率とLTVはnull、完全なライフサイクル履歴は未計測である。

19件のPython検証が成功した。完成稿レビューで見つかった「一部の正の分母が残りの未計測を隠す」問題を修正し、各指標で非ゼロ除外を常に記録する。限定再レビューで追加P1/P2は無かった。

次に必要なのは、同じ元の初回起動からの実観測・成熟・購読との対応と、課金/解約履歴の不足の解消。コードと空の集計が動いたことは、task11の業務完了の証拠にはならない。`ai_proposes` を維持し、blockerだけを `verification_pending` に更新する。日次ジョブは既存のものを継続し、追加の計測許可・新しい定期実行・native配布は作っていない。

## 返金通知

2026-09-10 05:09 UTC、本番D1の `apple_refund_events` を型・環境別に集計した。読取は成功、保持された通知は空、`rows_written=0`。これは保持対象の現在の観測であり、全期間に返金が存在しないという証明ではない。

受信・JWS検証・保持は既存実装を使う。実際の返金通知を受けた証拠がないため、task146は `nobody` / `verification_pending` のまま。テスト通知を返金に読み替えず、この記録のための新規購入や返金も行っていない。

両業務の詳細な集計証拠は非公開APIリポジトリとローカルの `AI-Execution-2026-09-10` に保存した。今回の記録更新はPR TIMESの上書き・予約・配信を行わない。

## 2026-09-11追記：収集停止の検出と実機からの受信確認

9月11日の本番確認で、購読の収集スイッチが停止していることを確認した。空の集計を自然なデータ待ちだけと扱うことはできない。API [PR #308](https://github.com/simplememofast/simplememo-api/pull/308) は、集計に照会時点の収集設定を加え、停止中または設定不明の場合は `verify_collection_readiness` を選ぶようにした。設定の観測は照会時点だけであり、過去の集計期間を通して収集が有効だったことは示さない。

同一head `5038b71c89f7b98992f47a9013dcabf15a53a4cd` のCIとプレビュービルド成功後、main `eb665854ea056218195da366c2fa33e08fda109e` へマージし、本番API `1.5.66` を確認した。関連する59件の単体テスト、21件のD1統合テスト、23件の評価テスト、10件の証拠検証テストと型検査が成功した。D1統合テストの署名検証部分は代替実装であり、実機の署名付きデータが届いた証拠には含めない。

一般向けの収集は停止したまま、所有者の既知のインストールと世代に限定した、30分で失効するSandbox確認枠を一度だけ設定した。別の実機担当が既存TestFlight `5.8.54 (1342)` を通常起動し、元の下書きが保持されたことを確認した。新規購入・復元・送信・消去・再インストールは行っていない。

その起動時間帯に、所有者の通常の利用記録は既知の世代と一致して保存された。一方、購読の保存記録はなく、取得したサーバーログにもその時間帯の購読APIへの到達記録は見つからなかった。画面のテスト成功を購読の送受信成功に読み替えない。通常の起動と利用記録の送信経路が動作した範囲まで確認できたが、購読候補の取得・途中の中断・購読用の通信のどこで止まったかは未特定である。

確認後に限定枠のsecretを削除し、一般向け収集の停止、限定枠の不在、受信停止の応答を本番で再確認した。下書きや共有する世代のデータは消去していない。[Retention Evaluation #34550017091](https://github.com/simplememofast/simplememo-api/actions/runs/34550017091) を同じmain SHAから一度実行し、認証付き本番集計と保存が成功した。artifactの出所とdigestも検証し、`owner_canary_active=false`、収集停止、`verify_collection_readiness` を確認した。これは手動起動の実行証拠であり、新しい定期実行の成功とは扱わない。

次の確認対象は購読producer固有の停止箇所である。一般向け収集の有効化、task11の完了、購入復元の成功、AI実行率への加点は行っていない。実機担当とAPI担当を分け、スコア台帳は変更していない。詳細証拠は非公開API artifactとローカルの `AI-Execution-2026-09-11/server-outcome-followup` に保持する。
