# 9月13日の日報からの修正

この記録は9月13日の調査結果。朝の日報には前日以前の実行記録と28日集計が混在していた。

## Autopilot

旧修理タスクの不明状態を後日の実行ロックとして引き継いだ停止は、既存の当日claim・共有PRに限定する修正と保存済み予約を照合した。本番のstatusは9/13のmaintenanceを確認。主系の診断は `skip_already_shipped` であり、記事公開の成功ではない。既存health workflow `34745735063` の成功後、監視issue #1264が閉じた。翌日の自然起動・記事公開は未検証。

## 問い合わせ・保存・Watch

API PR #319は、営業や自動通知を含む受信未処理件数を「利用者の問い合わせ」と誤表示しないための修正。元の要確認フラグを維持し、再分類は候補として表示する。個別メールの確認表と返信下書きは非公開の作業領域に保存し、送信・既存案件のクローズはしていない。

Obsidianの既存除外条件を使った再集計では、9/12・9/13の観測時点の追記失敗は0件、9/11は保存先参照読取失敗3件。過去の失敗が最終保存されたかは確認できない。設定の再選択など端末操作を実施済みとは扱わない。

Watchには9/13も7006の失敗記録があり、iOS PR #504で同期先不在時の代替転送・自動再試行を停止し、一時失敗の自動再試行を最大3回に制限した。ローカル全アプリのSimulatorビルドは成功。配信前の修正であり、利用者端末の復旧を意味しない。

## 日報の担当・期限

- 9/7の所有者判断でX/Redditは別担当が継続し、旧予約を変更しない。9/13の停止観測と照合して旧3件を理由・履歴付きの意図的停止へ分類した。後継の稼働・投稿成功は未確認。
- `not_before_jst` より前の未完了行は、要対応件数と最古滞留から外し、期日待ちに表示する。当日には担当欄へ戻し、滞留は着手可能日から数える。
- PR⑥の過去のhuman分類と判定履歴は保存し、委任済みの現在担当をAIにした。担当分類はこのスクリプトの無人実行権限を拡張しない。
- 旧 `simplememo-2` はPAUSEDを確認し、停止を維持した。既存ACTIVE監視 `ai-90` へ9/17のD+14取得・評価だけを追加し、保存後に読み戻した。別担当・PRとの重複確認、実測・確定評価・PR反映までの未完了条件を維持する。予約保存は評価完了ではない。

主系actionsの未出荷は実出荷でのみ閉じる。過去の故障行を整理した件数を、記事公開・投稿・アプリ復旧の件数へ読み替えない。
