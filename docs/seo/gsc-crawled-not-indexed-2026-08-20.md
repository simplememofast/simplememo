# GSC「クロール済み - インデックス未登録」63件 の追跡調査（2026-08-20）

対象レポート: ページのインデックス登録 →「クロール済み - インデックス未登録」
最終更新日 2026/08/17 / 該当 63件 / 検証: 失敗のまま（開始 2026/07/28、不合格 2026/08/05）

前回調査: `gsc-crawled-not-indexed-2026-08-11.md`（68件 → 61件は対応済み/対象外、
実問題7件に「2件統合・3件強化」を実施）

## 結論を先に

**68件 → 63件。前回の診断は維持。新規の欠陥はゼロ、新顔は `/en/guides/` 1件のみ。**

63件の内訳（前回と同じ分類基準、照合はBigQueryエクスポート実測 8/10〜8/17）:

| 区分 | 件数 | 状態 |
|---|---:|---|
| クエリ付き変種（`?lang=` / `?q=`） | 約33 | 全件、最終クロールが **2026/03〜05** ＝ 301実装（07-07）以前。**再クロール待ち** |
| `.html` 変種 | 約16 | 同上（最終クロール 2026/03〜06）。**再クロール待ち** |
| 301済み統合元（`memo-app-free-guide` / `line-keep-migration`） | 2 | 08-09/08-11 に301化。クロール日はそれ以前。**再クロール待ち** |
| 正規URL・実測で掲載あり | 3+ | `/glossary/timeboxing/`(直近8日は0だが3表示@08-09) 等、レポート滞留 |
| 正規URL・直近8日で表示ゼロ | **9** | 下記 |

変種URLのうち **301実装後にクロールされたものはゼロ**。つまりエッジ正規化に
穴は無く、GSCの検証失敗は前回の診断どおり「8日間の検証期間内に再クロールが
追いつかない」ことによる滞留。**サイト側に追加実装の余地は無い。**

## 直近8日で表示ゼロの正規URL 9件と対応

BigQuery `yurika-simplememo.searchconsole.searchdata_url_impression`
（データ最新日 2026-08-17）で表示回数ゼロだったもの。

| URL | 判断 | 今回の対応 |
|---|---|---|
| `/en/guides/` | #507（08-18）で新設、08/18クロール＝生後1日 | **原因を修正**（下記） |
| `/blog/business-memo-apps-2026` | 08-09時点7表示→直近0。被リンクは暗号化比較のみが強い | free-memo-apps-ranking（859表示）から追加リンク |
| `/blog/instant-capture-workflow` | 08-09時点2表示→直近0 | apple-watch/（347表示）から追加リンク |
| `/vs/todoist/` | 08-09時点1表示→直近0。最適な隣接元 email-self-task-management（118表示）は**既にリンク済み** | 追加なし（無理なリンクは規約違反）。経過観察 |
| `/blog/freelance-memo-management` | 被リンク4本（use-cases/freelancers 等クラスタ内で完結） | 同上。経過観察 |
| `/devlog/day1` | 開発日誌。about＋devlogクラスタが本来の生息地 | 同上。経過観察 |
| `/glossary/timeboxing/` | glossary相互リンク9本で技術的には健全 | 同上。経過観察 |
| `/vs/captioo/` | 08-11に強化済み（3→5本）。リンク元の再クロール待ち | 待つ |
| `/guides/draft-autosave/` | 08-11に強化済み（7→9本）。同上 | 待つ |

## 今回見つけた実欠陥: 新設ENハブが準孤立だった

#507 は `/en/guides/` `/en/vs/` `/en/use-cases/` のEN実ページを新設したが、
**既存 /en/ ページ45枚（ENホーム含む）のナビ・フッター・本文リンクは
JAハブを指したまま**だった。新設3ハブへの被リンクは相互リンク＋JA側の
言語切替のみ＝実質準孤立。`/en/guides/` が08/18クロールで未登録入りした
最有力の説明がこれ（クローラはJAハブへ流れ、ENハブに内部リンクの
シグナルが無い）。

/siri/ 分割（#494）後の既存規約「EN配下のリンクはEN版が実在すればENを指す。
JAを指すのは当該ページ自身の言語切替のみ」に揃えて全数貼り替えた。

- href 314本（/guides/ 111・/vs/ 106・/use-cases/ 100 から切替3本を除く）
- en/vs/ 配下13枚の BreadcrumbList JSON-LD（position-2 の item）も可視リンクと一致させた
- EN版が無い /methods/ /glossary/ /devlog/ は従来どおりJAのまま（locale seedが英語表示を担保）

コミット分割は意図的:
- 44ファイル一括（fdeb5d23）→ generate_sitemap.py のスイープ判定（>40）で lastmod 不変
- 実コンテンツ3ファイル（12af626c: en/index.html＋JAリンク2件）→ lastmod 2026-08-20 に更新

## サイトマップ再生成の注記

`/download/` の lastmod が再生成のたびに当日になるのは既存挙動
（唯一のコミット #489 が258ファイル＝スイープ判定のため、履歴上の
lastmod が無くフォールバック=TODAY になる）。/obsidian/ 3件の 08-19→08-20 は
#514 のマージ確定を正しく反映したもの（サイトマップはコミット前に
再生成されるため、常に1コミット分遅れる構造）。

## 残タスク（サイト外・オーナー操作）

1. **GSCで「検証を再開」を押す**のは変種の再クロールがある程度進んでから
   で良い（今押してもまた8日で不合格になる。前回と同じ理屈）。
   目安: 9月中旬に `growth/data/gsc` の日次で変種URLのクロールが
   観測されてから。押さなくても登録状況は同じ（検証はレポート表示の
   リセットに過ぎず、インデックス判定には影響しない）。
2. `/vs/todoist/` `/blog/freelance-memo-management` `/devlog/day1`
   `/glossary/timeboxing/` は9月の実測で再判定。表示ゼロが続くなら
   08-11方式の統合候補（オーナー判断事項）。
