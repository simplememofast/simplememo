# SEO改善計画 — 2026-09-05

対象snapshot: `0a4bd04c`。本計画は未実装。実装前に最新mainとの重複、実験台帳、事実の最新確認値を照合する。順位・DL改善の保証ではない。

## Critical

今回、サイト全体のindex遮断や実際の手動対策は確認されなかった。下記HIGHから着手する。

## High — 次の修正バッチ（目安1週間以内）

| ID | 修正範囲と処置 | 完了条件 | 規模 |
|---|---|---|---|
| H1 | 8locale + downloadの評価表示。定数同期にlocaleと括弧付きEN形式を追加 | 監査基準4.2/25、または更新された確認済み値に可視・JSON-LD・llmsが一致。翻訳表示を故意にずらす検査で失敗する | S |
| H2 | 旧速度50ファイル98候補を現行主張/過去測定/別操作に分類。特にiphone-memo-app-fastの順位・倍率・棒幅・metaを整合 | warm/cold、端末、測定日、操作の定義を明示。表の順位と倍率を再計算。歴史的測定を0.4秒へ機械置換しない | M |
| H3 | autopilotのdevlog向けcanonicalを削除。HTML parserでcanonicalを数える検査を追加 | 対象HTMLに自己参照canonicalがちょうど1本。本番HTMLも確認。Google選択は必要ならURL Inspectionで別確認 | XS |
| H4 | 8localeのhamburgerを実メニューにするか明確なダウンロードボタンへ変更 | 390pxで見えるラベルと挙動が一致。実メニューならキーボード、開閉状態、フォーカスも確認 | S |
| H5 | line-keepの7 EN FAQをJA schemaから除外。obsidian11ページは表示FAQと共通の生成元へ | 全Q&Aを表示内容と意味で照合。存在しない項目0。閉じたdetailsは開けば読めることを確認。schema構文成功だけを完了条件にしない | M |
| H6 | sitemapの170機械日付と8過少日付を履歴・本文更新と照合し修正。floorを例外なく現在日に寄せない | 138復元案はページ単位で根拠確認。未確定日付は省略も選択肢。CIが意味のある更新の未反映を検出し、変更のない再生成は差分0 | M |
| H7 | /obsidian/のfont-swap CLS。optionalと字幅調整を比較し適切な方を採用 | 同じcold-cache/ネットワーク条件で複数回測定しCLS≤0.1を目標。文字欠けなし。対象LPとiOS/WebKitも確認。field未取得を明示 | M |

H1/H3/H4は小さく独立した修正にできる。H2は最も矛盾の明確なランキングページから着手。H5はschema件数を合わせるためだけに本文を増やさない。H6はlastmodの復元が重要で、sitemap再送信は修復そのものではない。

## Medium — 次の1か月

| ID | 対象 | 処置・完了条件 |
|---|---|---|
| M1 | CSS source/min drift | minだけのscope=rowルールをsourceへ戻す。再生成後の意味上の差分0、代表表の表示維持、バージョン検査成功 |
| M2 | LP delta fonts | 対象ページの不足字形を生成しunicode-rangeも同期。欠落0、extが不要なLPで約266KBの追加取得が消えるか実測 |
| M3 | OG PNG22件 | 見た目を保って内部予算300KB以下を目標。生成経路にも圧縮を組み込む。画像上の文字・dimensions・参照URLを維持 |
| M4 | 誤ったSwiftUI表記、未出典の最上級・Captio原因推測 | UIKit等の一次資料と照合。only/guaranteesは立証範囲に限定するか削除。推測を事実扱いしない |
| M5 | 更新日・llms・運用数値 | facts変更時にLast updatedを更新。benchmarkの実質改訂日を一致させる。autopilotの各数値を公開snapshotと計算定義から辿れるようにする |
| M6 | 404 sameAs、無効speakable | 有効な公開公式URLへ整理、43ページのセレクタを検査。私有repoの公開は不要 |
| M7 | 比較の利害開示、use-cases内部リンク | 自社開発元であることを比較本文で明示。13ページのリンクを読者に有用な関係から増やす。本数達成目的で無関係なリンクを作らない |
| M8 | タイトル・説明 | JA114/EN14の長さ候補を検索語別に選別。進行中実験への変更を台帳へ記録。長いだけで一括改題しない |

## Low — 関連箇所を触る際に対応

- Cache-Controlの共通/個別重複を整理し、実応答を確認。認可のないpurgeやtoken変更は行わない。
- 2重table-scrollを単一スクロール領域にし、fadeと横はみ出しの両方を確認。
- localeの32px言語ボタンと37px項目を拡大、灰色pillや二重枠を整理。
- 古いheadline/可視日付/小さなSNSタイトル差分を個別確認。
- 短い用語解説は、足りない具体例や一次資料がある場合に増補。文字数だけをKPIにしない。

## 成長施策 — 既存実験に接続する

| 優先 | 対象と観測 | 次の判断 | 評価方法 |
|---|---|---|---|
| G1 | free-memo-apps-ranking: 6,034表示/36click | 無料・シンプルという意図への回答と比較基準を確認。既存selector実験を優先 | query/country/device/search typeを揃えたCTR・clickとCTAの実測 |
| G2 | line-keep: 6,514/42 | 終了とKeepメモの場所の質問に明確に回答。H5修正を記録 | FAQ/CTA既存実験と同じ期間・指標で評価 |
| G3 | EN shortcuts: 3,123/16、EN Keep比較: 1,425/4 | 英語検索語別に内容・導入を点検。JAとの差だけで改稿を決めない | 同条件で前後比較。十分な表示数を待つ |
| G4 | obsidian-voice-input: 511/48、CTR9.39% | 関連記事からの自然な内部誘導。進行中link施策を確認 | 検索clickとCTA、入手可能ならfirst-openまで分離計測 |
| G5 | capacities: 表示435→40、順位8.1→14.3 | running retitle実験のquery構成と日次推移を調査 | タイトルが原因とは決めず国・device・検索語の変化を比較 |

旧7月タイトル実験は台帳上評価済み（measurement_failedを含む）。実験を閉じたことと効果を測れたことは別。新たに重複実験を作らず、既存レコードの判断日と状態を使う。

21件の表示0ページは、URL Inspection、公開日、内部リンク、検索需要を合わせて調べる。business-memo-apps-2026やvs/captiooは候補として残すが、この24日だけで301や削除は実行しない。07-07→今回の点数変化と順位・DLの変化を因果関係として扱わない。

## 検証と引き渡し

1. 最新mainで各指摘を再現し、修正済み項目を除外する。
2. 修正対象に対応した既存CIと意味のある回帰検査を実行する。監査時にCI成功でも漏れたH1/H3/H5/H6/M1を優先。
3. UIは代表locale・JA/EN・モバイル/デスクトップ、fontはslow-network条件で確認。
4. デプロイ後は変更URLのみ本番確認。GSC反映待ちと修正コードの合格を区別する。
5. 実験は台帳の既存期限で評価。field CWVとアプリ成果が未取得なら未取得のまま報告する。

PSI/CrUX不足、App Storeの出荷時再確認、公開運用数値の出典が追加情報の必要箇所。いずれも他の独立したrepo修正を止める理由にはしない。
