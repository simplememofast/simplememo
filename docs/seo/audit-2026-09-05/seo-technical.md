# seo-technical — simplememofast.com full audit 2026-09-05

Inputs: `crawl/crawl.jsonl` (279 records: 276×200, 3×404; 267 unique HTML final URLs = 261 sitemap + 5 noindex + CF-Access login page), `crawl/pages/*.html`, checkout `wt/` @ 0a4bd04c, 46 counted live requests (≈50 actual incl. `-L` hops; sequential, 0.6 s apart, browser UA, no URL fetched twice).
Scripts + raw outputs: `work-seo-technical/analyze_crawl.py` (+`.out`, `parsed.json`), `link_check.py` (+`.out`), `live_probe.sh` (+`live_probe.out`, bodies), `faq_visibility.py` (+`.out`), `hidden_faq.out`.

## 1. Subscore: 93/100

07-07 baseline was 96. Every 07-07 technical action item (7, 13, 14, 20, sensitive-path block, font preloads, 2-hop chains) HELD on live + source. The deduction is for defects introduced by pages created after 07-07, exactly where the brief said to look: `/autopilot/` (the press landing page, 2026-08-25) ships **two `<link rel="canonical">`** (self + `/devlog/`), which makes Google ignore both — a real indexability defect on a flagship URL (−2). Structured-data plumbing on new pages regressed in kind: 8 new `obsidian/*` pages carry FAQPage questions that are paraphrases, not the visible text (19 Qs), and one older page (`blog/line-keep-alternative`) still has 7 EN Question objects that exist only inside a hidden `data-lang="en"` block with no EN sister — the invisible-content class the 07-07 item 2 recipe was meant to eliminate (−1, shared with the schema agent for dedupe). Everything else — canonical parity on 266/266 own pages, 0 hreflang faults across 83 clustered pages, 0 broken internal links out of 10,640, 0 missing images/preloads, uniform security headers, single-hop edge normalisation for every variant tested, sensitive paths closed including the `//` bypass — is clean. The `_headers` Cache-Control merge on 4 non-page paths is LOW and does not move the score.

## 2. Held / regressed from 07-07

| 07-07 item | Verdict | Where checked |
|---|---|---|
| #7 Sensitive-path block (`/CLAUDE.md`, `/scripts/*`, `/tools/.env.example`) + stale comment :32-36 | **HELD** (extended: `/fixtures`, `/data/*.json` ×45, `//` collapse before the block) | live HEAD → 404 + `x-robots-tag: noindex` + `cache-control: no-store` for `/CLAUDE.md`, `/scripts/seo-check.js`, `/tools/.env.example`, `/fixtures/engine-divergence.html`, `/data/site-constants.json`, `//docs//seo/` (live_probe.out [32]-[37]); `wt/functions/_middleware.js:162,246-274`; comment rewritten at :25-34 |
| #8 Cargo-cult font preloads on 10 inline-CSS pages | **HELD** | crawl: 252 pages preload the 2 subset fonts = exactly the 252 pages loading `style.min.css`; all 14 inline-CSS pages have 0 font preloads (`analyze_crawl.out` PRELOADS; `link_check.out` PRELOAD unused = 0) |
| #9 CF Web Analytics beacon blocked by CSP | **HELD** | `wt/_headers:12` script-src includes `https://static.cloudflareinsights.com`, connect-src `https://cloudflareinsights.com`; beacon `<script>` present on 266 crawled pages |
| #13 Residual font preloads on 15 EN pages | **HELD** | crawl: every `style.min.css` page (incl. 12× `en/vs/*`, `en/faq`, `en/about`, `en/blog/best-memo-apps-2026`) preloads `NotoSansJP-Regular-subset.woff2` + `NotoSansJP-Bold-subset.woff2?v=2` |
| #14 2-hop chains `/blog/captio-alternatives-comparison.html`, `/blog/memo-shuukan-tips.html` | **HELD** | live: `/blog/memo-shuukan-tips.html?lang=ja` → 1×301 → `/blog/memo-habit` final 200 ([11]); crawl: `/blog/captio-alternatives-comparison.html` → 1×301 → `/captio-alternative/`; `_middleware.js:183-205` RETIRED applied after `.html` strip (:297-312) |
| #16 og/tw title sync (`vs/google-keep/`, `en/obsidian/`, `en/vs/google-keep-vs-apple-notes`) | N/A (on-page agent) | — |
| #20 Robots-meta stragglers ×18 (15 missing, 3 bare) | **HELD** — 0 missing, 0 bare on 266/266 own pages | `analyze_crawl.out` ROBOTS META: 198 `index,follow,max-image-preview:large,max-snippet:-1` / 42 `max-snippet:-1,max-image-preview:large` / 21 same directives, other order / 5 `noindex,nofollow` / 1 `noindex` (CF Access page). Residual = 3 spellings, consistency only |
| INFO `_middleware.js:32-36` stale comment | **HELD** (fixed) | `wt/functions/_middleware.js:25-34` now correctly describes the `http://www` extra hop |
| Sensitive `/docs/`, `/growth/`, `/admin/` | **HELD** | crawl: `/docs/` 404, `/growth/` 404 (both `x-robots-tag: noindex`), `/admin/` 302 → Cloudflare Access login |

## 3. Findings

| Sev | Location | Issue | Evidence | Verified | Fix |
|---|---|---|---|---|---|
| HIGH | `autopilot/index.html:50-51` (live `https://simplememofast.com/autopilot/`) | Two canonical tags on one page; conflicting canonicals are ignored by Google, so the press landing page has effectively no canonical. Only page site-wide with ≠1 canonical (count = 1). | `:50 <link rel="canonical" href="https://simplememofast.com/autopilot/">` `:51 <link rel="canonical" href="https://simplememofast.com/devlog/">` — both lines from commit a7e4141b (2026-08-25); `devlog/index.html:50` carries the identical `/devlog/` line (clone artifact). Live crawl `canonical` list = `['…/autopilot/', '…/devlog/']`. | source + live crawl | Delete `autopilot/index.html:51`. Add an exactly-one assertion to `scripts/seo-check.js:194` (today `content.includes('rel="canonical"')` = presence only). |
| HIGH | `blog/line-keep-alternative.html:233` (FAQPage) + `:907-908` | FAQPage lists 14 Questions; 7 are English and their text exists only inside `<div lang="en" data-lang="en">` (hidden by `style.css:2512 [data-lang="en"]{display:none}`); no EN sister page exists (`en/blog/line-keep-alternative.html` absent). Invisible-content class per brief; the 07-07 item-2 recipe missed this page. | JSON-LD `mainEntity` = 14 (7 JA + 7 EN: "Where is LINE Keep Memo?", "Can I migrate my LINE Keep data?", …); `:907 <div lang="en" data-lang="en">` → `:908 <summary class="faq-summary">Where is LINE Keep Memo?</summary>`. Site-wide this is the only page with hidden-EN Questions (`faq_visibility.out`: hidden = 7, all on this page). | source (default-visible DOM method: drop `data-lang="en"` elements, scripts, styles; whitespace/punctuation-insensitive match) | Delete the 7 EN Question objects from the FAQPage (delete-only, same as PR #369 recipe). Shared with schema agent — dedupe there. |
| MEDIUM | 8 new pages: `obsidian/shortcuts-not-working/index.html:74` (8/8 Qs), `obsidian/getting-started/:74` (5/7), `obsidian/daily-note/` (4/8), `obsidian/apple-watch-not-working/` (3/8), `obsidian/what-is-vault/` (2/5), `obsidian/plugins/` (2/7), `obsidian/compare/` (1/5), `obsidian/compare/logseq/` (1/6) — count 8 pages / 26 Qs | FAQPage `Question.name` strings are paraphrases of the visible headings/summaries, not the visible text (Google: FAQ content must be visible verbatim on the page; also breaks the answer-layer parity standard set 07-07). All 8 pages were created 2026-08-11…08-21. | `obsidian/shortcuts-not-working/`: JSON-LD `"ショートカットを実行するとObsidianが前面に開いてしまいます"` vs visible H3 `:455 実行するとObsidianが前面に開いてしまう`; JSON-LD `"別の保管庫に書き込まれてしまいます"` vs H3 `:495 別の保管庫に書き込まれる／保管庫が見つからない`; the page's visible `.faq-item` set (5 Qs, e.g. "ショートカットとアプリ内蔵アクション、どちらを使うべきですか？") is a different list from the JSON-LD (8 Qs). `obsidian/getting-started/`: JSON-LD `"Obsidianを始めるのにアカウント登録は必要ですか？"` vs `:557 <summary>…アカウント登録は必要ですか？`. Full list in `work-seo-technical/faq_visibility.out`. | source (same method as above) | Regenerate `Question.name` (and `acceptedAnswer.text`) from the served `.faq-summary`/`.faq-answer` innerHTML with the 07-07 item-3 script; where the JSON-LD Q has no visible FAQ item (shortcuts page), either add the visible FAQ item or drop the Q. Shared with schema agent. |
| LOW | `_headers:32` vs `:56-70,86-88,112-121` (live `/sitemap.xml`, `/sitemap-ja.xml`, `/sitemap-en.xml`, `/sitemap-locales.xml`, `/llms.txt`, `/verify`, `/compose`) | Since PR #813 put `Cache-Control` on `/*`, the per-path Cache-Control rules are appended, not replaced, producing a duplicated header (the additive trap `_headers:50-53` itself documents). Effective directive is `no-cache`, so the intended 1 h/24 h caches are void; harmless to indexing. | live: `cache-control: public, no-cache, public, max-age=3600` on all 3 child sitemaps ([28]-[30]); crawl: `/sitemap.xml` same, `/llms.txt` `public, no-cache, public, max-age=86400`, `/verify` + `/compose` `public, no-cache, public, max-age=3600`. `/robots.txt` is clean (`public, max-age=86400`). Assets are clean: `style.min.css` → `public, max-age=604800, immutable` ([43]). | live fetch + live crawl | Delete the `Cache-Control:` lines under `/sitemap*.xml`, `/llms.txt`, `/verify`, `/compose` (they inherit `public, no-cache`), or move them to a Cloudflare Cache Rule; confirm on a preview deploy. |
| LOW | 16 of 34 pages created after 07-07 (`autopilot/`, `download/`, `fastest-voice-memo/`, `blog/google-keep-shutdown`, 12× `obsidian/*` — list in `analyze_crawl.out`/new-page sweep) | Built dual-DOM (inline `<span data-lang="ja">/<span data-lang="en">` pairs, 50–336 elements each, no EN sister, no hreflang) against the house convention "new pages are plain single-language static". Not an indexing defect (JA is default-visible via CSS) but it is the pattern that produced the MEDIUM above and doubles page weight. | e.g. `obsidian/shortcuts-not-working/`: 336 `data-lang` elements, hreflang = 0; `download/`: 112; `fastest-voice-memo/`: 262. Checkout-wide dual-DOM count is now 189 files (was 158 at 07-07). | source + crawl | No mechanical fix now; add a CI guard that rejects `data-lang=` in files first-committed after 2026-07-07 unless an EN sister exists, and strip the EN spans on the next touch of each page. |
| LOW | `data/voice-shift/index.html`, `obsidian/airpods/index.html` | Load `/js/lang.js?v=25811b2b9a` but contain no `data-lang` element — dead 6.5 KB script + 1 request on 2 new pages. | crawl `scripts` includes `lang.js`, `datalang` = 0 for both (`analyze_crawl.out` "lang.js but no data-lang"). | live crawl | Remove the `<script src="/js/lang.js…">` tag on both. |
| LOW | `captio-alternative/index.html:1421,1424,1427` | 3 `<span data-lang="en">` pairs but the page does not load `lang.js` → the EN spans are permanently hidden dead text. | `grep -c 'data-lang=' captio-alternative/index.html` = 3; crawl `scripts` = ahrefs, app-store-tracking, beacon only. | source + crawl | Delete the 3 EN spans and their `data-lang="ja"` wrappers. |
| INFO | `functions/_middleware.js:270-273, 317-324` | Function-built 404/410 responses (`/docs/…`, `/growth/…`, `/CLAUDE.md`, GONE slugs) carry no CSP/HSTS/XFO/XCTO/RP/PP — `_headers` does not apply to `Response` objects created in a Function. Only `Cache-Control: no-store` + `X-Robots-Tag: noindex`. | crawl: `/docs/`, `/growth/` → `csp:false, hsts:false, xfo:null`; live [25] 410 same. | live crawl + live fetch | Optional: add the HSTS/XCTO/XFO lines to the two `headers:{…}` literals. |
| INFO | live `/404.html` → 308 → `/404` → 200 | Pages' native `.html`→extensionless 308 also applies to the error document, so `/404` serves the 404 page with status 200 (`noindex, nofollow`, no canonical, no og:url). Middleware exempts `/404.html` from its own strip (:297) but cannot stop the Pages 308. Harmless. | crawl record: hops `[(308,'/404')]`, status 200, robots `noindex, nofollow`. Real 404s are correct: `/nope-audit-2026-09-05` → 404, body byte-identical to `404.html`, `<meta name="robots" content="noindex, nofollow">` ([26]). | live crawl + live fetch | None required. |
| INFO | `scripts/seo-check.js:194, :400, :517` | Checker blind spots: canonical rule is presence-only (missed the HIGH above); `href="…"` regexes skip `href='…'` (1 internal instance: `vs/google-keep/index.html` → `/blog/memo-app-service-shutdown-risk`, target exists) and skip protocol-relative `//` hrefs (0 in source). Query strings and fragments ARE handled (`:416 split('#')[0].split('?')`). | independent resolver over 269 files: 10,640 internal hrefs → 0 broken; 0 www; 0 `?`; 0 `.html`; 0 retired/410 targets; 0 dir-without-slash (`link_check.out`). | source | Add `(content.match(/rel="canonical"/g)||[]).length===1`; extend the href regex to `href=["']`. |
| INFO | `assets/css/style.css:216` | `body{overflow-x:hidden}` is dead — superseded by `:2827 body{overflow-x:clip}` (later declaration wins). If the order ever flips, `hidden` creates a scroll container and kills the sticky nav (the comment at :2820-2826 explains why `clip`). | both selectors are bare `body`; `clip` is last. | source | Delete `:216`. |
| INFO | live edge cache | HIT assets still serve the pre-#813 merged header shape (`/js/lang.js` `public, max-age=604800, must-revalidate, immutable`; font `public, max-age=31536000, must-revalidate, immutable`; `og/index.png` same as lang.js). Pages deploys do not purge the edge cache; REVALIDATED `style.min.css` already shows the clean form. Harmless (`immutable` + fresh `max-age` win). | live [43]-[46] with `cf-cache-status`. | live fetch | Owner: one-off cache purge (token cannot purge). |
| INFO | `templates/index.html` | hreflang cluster is `ja` + `x-default` both → self (the only 2-entry cluster); pointless but valid. | crawl hreflang `[['ja','…/templates/'],['x-default','…/templates/']]`. | live crawl | Drop both tags or leave. |
| INFO | live `http://www.simplememofast.com/blog/memo-habit.html?lang=en` | 2 hops (edge Always-Use-HTTPS 301 → `https://www…/…html?lang=en`, then middleware 301 → final). Documented at `_middleware.js:31-34`; out of repo control; every https/apex spelling tested is 1 hop. | live [5]: `chain: 2 redirects`; all other 11 variants `chain: 1`. | live fetch | None (Cloudflare edge). |

## 4. Verified clean

- **Canonical**: 266/266 own HTML pages self-canonical = final URL (0 mismatches, 0 missing except `/404`); the only multi-canonical is the HIGH above. og:url == canonical on all 263 pages that have og:url (missing only on 404/verify/compose, all noindex).
- **Noindex set = expected 8**: live `noindex,nofollow` on `/404`, `/verify`, `/compose`, `/tiktok/`, `/en/blog/revenue-report-2025` (+ `x-robots-tag: noindex, nofollow` on verify/compose); `admin/index.html:6`, `admin/brand/index.html:6`, `admin/reddit/index.html:6` in source (live is Access-gated 302). No sitemap URL is noindex; all 261 sitemap URLs are live 200 + self-canonical; no indexable 200 page is outside the sitemaps; checkout file set (269) − 8 noindex = 261 = sitemap.
- **Robots meta**: present on 266/266 own pages, parsed attribute-order-independently; no page has two robots tags.
- **html lang vs path**: 0 mismatches (ja 210, en 48, es/ko/zh-Hans/zh-Hant/ar/id/pt-BR/tr 1 each).
- **hreflang**: 83 pages carry it (10×11-entry home clusters, 72×3 ja/en/x-default, 1×2); 0 defects — every target is a 200 self-canonical page in the crawl, every pair is bidirectional, x-default present and equal to one of the alternates, codes all valid, no duplicates, self-reference present. Sister completeness: 0 pages whose sister URL is live but absent from their hreflang. The 10 EN pages without hreflang have no JA sister (by design), as do ~165 JA pages.
- **Titles/H1**: 0 duplicate titles across 267 pages, 0 missing, 0 >70 ch, 0 <20 ch; exactly 1 H1 on every indexable page (only `/404` has none). 0 JSON-LD parse errors.
- **Security headers**: CSP + HSTS (`max-age=31536000; includeSubDomains; preload`) + `X-Frame-Options: DENY` on 277/279 crawl responses (missing only on the 2 Function-built 404s, INFO above); `Access-Control-Allow-Origin` absent on every HTML response (present only on `/assets/*`, by rule); `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` verified on 12 live HEADs (5 new pages + 4 assets + 3 sitemaps; the `/*` rule is uniform). No `http://` sub-resources in any file.
- **Cache-Control**: 270/270 HTML 200s serve `public, no-cache` (deliberate, PR #813); `/robots.txt` `public, max-age=86400`; the 4-path merge is the LOW above.
- **robots.txt**: live body byte-identical to `wt/robots.txt` (3394 B), `text/plain; charset=utf-8`; Disallows only `/admin/ /docs/ /growth/ /fixtures/ /cdn-cgi/`; parameter URLs deliberately not blocked (handled by 301/canonical); `Sitemap:` present; 4 scrapers (CCBot/Bytespider/Amazonbot/Diffbot) fully disallowed; AI search agents allowed.
- **Sitemaps**: index + 3 children all `application/xml; charset=utf-8`, 200; 206/47/8 = 261 locs; index lastmods 09-03/09-03/09-02.
- **Edge normalisation (live, 1 hop each)**: www→apex; `http://`→https; `index.html` strip; `?ref=` strip; `.html?lang=` fold + retired map; `/vs/mem` (no slash, via `_redirects`, relative `Location: /vs/`) and `/vs/mem/index.html`; `/privacy-policy/`; `//` collapse; `/en/index.html?lang=en&ref=x`; `/)`; `/en` → 308 `/en/` (Pages). `utm_source` passes through as 200 (by design). GONE slug → 410 + `x-robots-tag: noindex`. Rules enumerated: middleware RETIRED 15 entries, GONE 6, INTERNAL_PREFIXES 5, UNSERVED_DATA 45; `_redirects` 27 path rules — every `_redirects` target is mirrored in RETIRED, no conflicting pairs, no rule targets a redirecting URL, none of the retired sources exist as files.
- **Internal links/images/preloads (source, 269 files)**: 10,640 internal `<a href>` → 0 unresolved (resolver honours `?`/`#`, single/double quotes, absolute self-links, protocol-relative, directory vs `.html`, middleware rules); 0 `<img>`/`srcset`/`<source>` missing files; every `<link rel=preload>` target exists and is used (fonts via `style.min.css`/inline `@font-face`, images referenced ≥1× outside the preload); 0 missing CSS/JS.
- **JS rendering**: `js/lang.js:20` captures `SERVED_LANG` from `<html lang>` before mutation and `:120-122` only reads `.meta-template` when `lang !== SERVED_LANG` — the 06-16 title-clobber fix holds. No `/en/*` or locale page loads `lang.js` or has `data-lang`, so `DEFAULT_LANG='ja'` cannot flip EN content for a JS-executing crawler without localStorage. On the 169 JA dual-DOM pages JA is default-visible by CSS (`style.css:2512`), so indexable content does not depend on JS. `roadmap/` + `en/roadmap/` `fetch()` is a POST to `/api/survey` with a `<noscript>` fallback (`roadmap/index.html:488`); no page injects primary content via `fetch`/`innerHTML`.
- **Mobile**: viewport on 266/266 own pages (264 `width=device-width, initial-scale=1, viewport-fit=cover`; verify/compose `initial-scale=1.0`); no `user-scalable=no` on any own page. CSS posture after #814/#824: `keep-all` fully gone (0 occurrences outside a comment); body/headings `word-break: normal` with `auto-phrase` progressive enhancement (`style.css:204-205, 224-225`), `line-break: strict`, `overflow-wrap: break-word` (:207, re-asserted :2740), flex/grid children `min-width: 0` (#824), `body{overflow-x:clip}` (:2827), `.nb{white-space:nowrap}` for product-name chunks; the 17 pages without `style.min.css` (14 public + 3 admin) now load `safety-net.css?v=b380c7e070`. `check-css-version.mjs` OK, min/source in sync. Rendering itself is the visual agent's call.
- **New-page sweep (34 files first-committed after 2026-07-07)**: all 200, in sitemap, `index,follow` full robots variant, `html lang` matches path, 1 title + 1 H1, standard viewport, og:url == canonical, full security header set on the 5 probed; 14 have valid ja/en hreflang pairs, 20 are JA-only. Defects found: the `/autopilot/` canonical (HIGH), FAQ paraphrase drift on 8 (MEDIUM), dual-DOM ×16 / dead `lang.js` ×2 (LOW).

## 5. Owner-only actions

- One-off Cloudflare edge cache purge after the `_headers` cleanup so HIT assets stop serving the pre-#813 merged `must-revalidate, immutable` header (the repo token cannot purge; Pages deploys do not purge).
- Nothing else in the technical domain requires dashboard access; `/admin/` Cloudflare Access gate verified live (302 to `simplememo.cloudflareaccess.com`).
