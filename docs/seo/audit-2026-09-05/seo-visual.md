# seo-visual — rendered/visual audit, simplememofast.com, 2026-09-05

Method: Playwright 1.58 Chromium (1217), 16 pages × 2 viewports (desktop 1280×800; mobile 390×844, iPhone UA, DSF 2, isMobile), loaded sequentially ≥1.2 s apart, GA/Ahrefs/CF-Insights aborted by route. 3 re-check loads (`/blog/open-source-memo-apps`, `/en/captio-alternative/`, `/faq`) + 2 deliberate repeat loads of `/` (scroll-through for a paintable full-page capture). 35 loads, 358 same-origin requests incl. subresources, 0 × 429/4xx. WebKit is not installed on this Mac (`webkit-2248` missing; #825 installed WebKitGTK on Linux CI only) — cross-check skipped. Evidence dir: `scratchpad/work-seo-visual/` (`results.json`, `analysis.txt`, `<slug>-<vp>.png` full page, `<slug>-<vp>-fold.png`, `*-mobile-menu.png`, `*-mobile-lang.png`, `sheets/*`).

Overflow method (PR #810 class): `scrollWidth` is blind on this site — `body{overflow-x:clip}` (`assets/css/style.css:2827`) and `content-visibility:auto` (`:1671`) both hide it (local test `work-seo-visual/cvtest.html`: a 900 px element inside a skipped c-v section → `scrollWidth` 390, `rect.right` 900). All overflow numbers below are element enumeration: every element with `getBoundingClientRect().right > innerWidth+1`, classified by whether an ancestor (excluding html/body) has `overflow-x` auto/scroll/hidden/clip.

## 1. Subscore: 87/100

07-07 visual component was 92. The headline claims hold on the sample: 0 of 16 pages have unclipped horizontal overflow at 390 px (and at 1280), every table on the 16 pages sits in an `overflow-x:auto` container (0 leaks, incl. the 07-07 `/captio-alternative/` item), H1 above the fold 32/32 loads, App Store badge above the fold on all 7 conversion pages, body text 15.4–16.3 px, contrast 7.05–19.7:1 on body/H1/CTA, hamburger opens a 10-row 48 px menu with no overflow, lang dropdown 10 × 110×44 within viewport, 0 broken images, 0 mojibake, CLS API 0–0.008. Points come off for two newly found real defects on indexable pages — the 8 locale homepages' "Menu" ☰ button that navigates to the App Store with no nav at all (HIGH, 8 pages), and a stale visible rating "4.4" contradicting the same pages' JSON-LD 4.2/25 (HIGH, 9 pages) — plus one 07-07 regression: PR #824's bulk table wrap nested a second scroller inside the two `.table-scroll` edge-fade wrappers, so the 07-07 swipe affordance no longer renders (LOW, 2 pages). The rest is LOW cosmetic/tap-target backlog.

## 2. Held / regressed from 07-07

| 07-07 item | Status | Checked |
|---|---|---|
| #4 `/en/apple-watch-obsidian/` hero image | HELD | `en/apple-watch-obsidian/index.html:329` `<img src="/assets/img/apple-watch-obsidian-hero.jpg" width="1200" height="900">` |
| #9 CSP blocked CF Insights beacon (1 console error/page) | HELD (fixed) | `_headers:12` script-src has `https://static.cloudflareinsights.com`, connect-src `https://cloudflareinsights.com`; 0 CSP console errors in 32 loads (only my own route aborts logged) |
| #17 `/captio-alternative/` 841 px table → `.table-scroll` | HELD | `captio-alternative/index.html:1114`; live 390 px: `table.compare-table` w=1113 inside `div.table-scroll` overflow-x:auto, container scroll 763 px, 0 unclipped leak |
| #18 OSS article mid-body CTA | HELD | `blog/open-source-memo-apps.html:288` `data-cta-placement="mid"` (+ nav :233/:236, bottom :334) |
| OSS `.table-scroll` edge-fade rendering | **REGRESSED** | see Finding 4 — `blog/open-source-memo-apps.html:268-269` `.table-scroll` > `div[style=overflow-x:auto]`; live `.table-scroll` scroll = 0 px, inner div = 424 px |
| lang-switcher pill label offset −0.6 px | HELD | `/faq` 390 px: `.lang-switcher__btn` JA 36.4×44 / EN 36.2×44, label offset Y −0.6, X 0; `style.min.css` carries the `inline-flex; align-items:center` rule |
| Homepage lang toggle 44 px | HELD | `.lang-dropdown__toggle` 48.5×44 (`/`), 49.8×44 (`/en/`), vertical offset vs nav 0 |
| vs/capacities nav 0 wraps | HELD (as class) | desktop nav 1 row, 9 `li` on 16/16 pages |
| 0 px horizontal overflow everywhere sampled | HELD | 32/32 loads: 0 unclipped elements; #814 `word-break:keep-all` class: 0 keep-all setters on any page; nowrap-JA setters wider than viewport exist only inside table cells within scroll containers |
| App Store badge above fold on conversion pages | HELD | 390 px badge top: `/` 449, `/en/` 465, `/obsidian/` 627, `/apple-watch/` 552, `/captio-alternative/` 446, `/download/` 497, `/ko/` 227 |
| Pill links 36×44 (INFO) | unchanged | still 36.4×44 — tall enough, narrow |

## 3. Findings

| Sev | Location | Issue | Evidence | Verified | Fix |
|---|---|---|---|---|---|
| HIGH | `ko/index.html:264` + same button in `es/ zh/ zh-Hant/ ar/ id/ pt-BR/ tr/index.html` (8 pages) | The mobile "Menu" hamburger is not a menu: it navigates to the US App Store, and the locale pages have no nav links at all, so the only visible nav control at ≤1099 px is a mislabeled off-site link. | `<button class="global-nav__hamburger" aria-label="메뉴" onclick="window.location.href='https://apps.apple.com/us/app/id6758438948'">☰</button>`; `id="navLinks"` count 0 on all 8; `.global-nav__cta { display:none }` in the ≤1099 px media block (`ko/index.html:164`); rendered ☰ 48×48 at 390 px, `menu=null` (`ko-mobile-fold.png`, `ko-desktop-fold.png`) | source + rendered | Replace the ☰ with a visible localized Download CTA (show `.global-nav__cta` on mobile, or relabel the button "Download"/"다운로드" with `aria-label` matching), and drop the hamburger glyph. Optional: locale storefront (`/kr/`, `/es/`…) instead of `/us/`. |
| HIGH | `es/ ko/ zh/ zh-Hant/index.html:285`, `ar/index.html:329`, `id/ pt-BR/ tr/index.html:319`, `download/index.html:268` (9 pages) | Visible rating text is stale and contradicts the JSON-LD on the same page and `data/site-constants.json` (4.2 / 25) — the 3-point sync is broken outside the sync markers. Arguably CRITICAL by the brief's "false product fact" rule; rated HIGH because JSON-LD and llms.txt are correct. | 8 × `★ 4.4 · … 21 …` (e.g. ko: `★ 4.4 · App Store 평가 21개`) while the same files carry `"ratingValue": "4.2" "ratingCount": "25"`; `download/index.html:268` JA span `App Store 4.2（25件の評価）` vs EN span `App Store 4.4 (25 ratings)`; `llms.txt:8` `4.2 (25 rating`. Rendered: `ko-mobile-fold.png` "★ 4.4 · App Store 평가 21개", `download-mobile-fold.png` "App Store 4.4 (25 ratings)". `sync_constants --check` passes → these strings are outside its markers. | source + rendered | Put the 9 strings under the sync markers / regenerate from site-constants; add a CI grep (`★ ?4\.4|4\.4 \(|App Store 4\.4`) to `sync_constants --check`. |
| MEDIUM | `ko/index.html` inline `<style>` (`.lang-dropdown__toggle` / `.lang-dropdown__menu a`), same template on the 8 locale pages | Locale lang toggle and its menu rows are under the 44 px tap minimum, unlike the root template. | 390 px: `.lang-dropdown__toggle` 51.1×**32**; dropdown links 110×**37** (root: 48.5×44 and 110×44) | rendered | Copy the root padding (`min-height:44px`) into the locale inline rule. |
| LOW (regression) | `blog/open-source-memo-apps.html:268-269`, `en/captio-alternative/index.html` (`.table-scroll` > `div[style="overflow-x:auto"]`) | PR #824's bulk wrap nested a second scroller inside the 07-07 `.table-scroll` fade wrapper; the fade uses `background-attachment: local,local,scroll,scroll`, which only engages when `.table-scroll` itself scrolls — now it never does, so the swipe cue is gone and the table hard-cuts at the edge. | live 390 px: OSS `.table-scroll` scrollWidth−clientWidth = **0**, inner div = **424** (table 774 px); en/captio: 0 / **343** (table 693); `blog_open-source-memo-apps-mobile-table.png` shows a hard cut at "最終リリース" with no gradient. Third double-wrap `fastest-voice-memo/index.html` has no fade CSS (harmless). | source + rendered | Delete the inner `<div style="overflow-x:auto">` on the 2 fade pages; make `check-viewport-overflow` treat a `.table-scroll` ancestor as already wrapped. |
| LOW | `captio-alternative/index.html:801` (`.page-content .lp-hero__label * { … background: … !important; border: 1px solid rgba(77,163,255,.30) !important; }`) applied to the `<span>` at `:906` | The eyebrow pill renders a second rectangle inside the pill (box-in-pill); the same `.lp-hero__label` on `/vs/`, `/download/`, `/apple-watch/` renders one frame. | `captio-alternative-mobile-eyebrow-crop.png`, `captio-alternative-desktop-fold.png` | source + rendered | Scope the `*` rule to `a, b` or drop `background`/`border` from it. |
| LOW | `download/index.html`, `line-keep/index.html`, `contact.html`, `legal.html`, `blog/iphone-memo-katsuyou.html`, `devlog/privacy-first-design.html`, `devlog/relay-api-design.html`, `devlog/uikit-vs-swiftui.html` (8 of the 165 `<button class="lang-switcher__btn">` pages) | Inactive JA pill shows the UA `buttonface` grey fill (light-grey label on mid-grey ≈ 2:1) because the shared `.lang-switcher__btn` rule (`assets/css/style.css:2321-2350`) only sets `background` for `.active`; 157 pages mask it with an inline reset, these 8 do not. | `download-mobile-langpill-crop.png`, `download-desktop-fold.png` (grey JA circle) vs `autopilot-mobile-langpill-crop.png` (transparent). List: `work-seo-visual/grey-pill-pages.txt` | source + rendered | Add `background: transparent; border: 0;` to the shared `.lang-switcher__btn` rule (source + min). |
| LOW | `/`, `/en/`, `/ko/` hero (`assets/css/style.css:109-162` `@font-face … font-display: swap`, fallback `size-adjust: 100%`) | Hero badge/CTA move up 25 px between 0.5 s and 3 s after load (one subtitle line at 15.2 px × 1.5 ≈ 23 px) — the Noto JP swap re-wraps the subtitle on a cold cache despite the preloads. | `box500→box3000`: hero `[0,-25,0,0]`, cta `[0,-25,0,0]`, h1 `[0,-2,0,0]` on all three; PerformanceObserver layout-shift total 0 in this headless run, so this is a first-visit CLS risk to confirm in CrUX, not a measured CLS. | rendered | `font-display: optional` for the JP subset, or a metric-matched fallback (`size-adjust`/`ascent-override` tuned to Hiragino Sans) so line count is stable. |
| LOW | `captio-alternative/index.html` `nav.lp-toc > ol > li > a` (6 links) | TOC links are 20 px tall, stacked, no padding — under the 44 px tap minimum. | 390 px: 262×20, 234×20, 247×20, 169×20, 228×20, 65×20 | rendered | `.lp-toc li a { display:block; padding:.6rem 0 }` |
| INFO | 9/16 pages at 390 px (`/autopilot/`, `/obsidian/plugins/`, `/obsidian/getting-started/`, both blogs, `/vs/notion/`, `/vs/`, `/roadmap/`, `/glossary/e2e-encryption/`) | No CTA above the fold on mobile: `.global-nav__cta` is hidden ≤1099 px and the CTA lives in the hamburger (ダウンロード row, 48 px). Matches the 07-07 convention (badge above fold on conversion pages only). | `ctaAF body=[]` on those 9; menu row `nav-cta-mobile` 48 px | rendered | none |
| INFO | `download/index.html` nav | Nav template drift: `/download/` lacks the 開発記録 (Dev Log) item — 9 menu rows vs 10 elsewhere. | `grep -c '<li><a href="/devlog/">'` = 0 vs 1 on `index.html`; `menu.linkCount` 9 | source + rendered | Re-sync the nav block. |
| INFO | `/autopilot/` (dual-DOM shell, JA-only body) | With `lang=en` persisted (my context had visited `/en/` first; any visitor who once chose EN), the shell flips to EN (pill EN active, nav labels EN, brand "Simple Memo - for Obsidian") while the body stays JA. Architectural, not per-page; Googlebot (empty localStorage) sees JA. | `autopilot-desktop-fold.png`, `autopilot-mobile-langpill-crop.png` | rendered | Hand to technical/AI agents; no visual fix. |
| INFO | `/` 390 px `.pricing-cards` | Pricing is a horizontal carousel: cards at x 20–180 / 192–352 / 364–524 (third peeks 26 px, scroll 154 px); annual price needs a swipe. Affordance present. | `root-mobile-scrolled-pricing-crop.png` | rendered | none |

## 4. Verified clean

- Horizontal overflow (headline): **0/16 pages fail at 390 px**, 0/16 at 1280 px — 0 unclipped elements on 32/32 loads; every `right > innerWidth` element is a table cell inside an `overflow-x:auto` wrapper (e.g. `/autopilot/` 212, `/vs/` 192, `/captio-alternative/` 129 clipped elements, all inside `div:auto` / `.table-scroll`). PR #824's claim holds on this sample in Blink; the #814 bug class (`word-break:keep-all`) has 0 setters on any page; PRESS band (#802) wraps into 4 lines at 390 px (`root-mobile-scrolled-press-crop.png`); nowrap-JA setters wider than the viewport exist only in table cells inside scrollers (`/` `.pricing-card__price-row` 122 px is inside the carousel).
- Tables: 39 tables on the 16 pages, all inside `overflow-x:auto` (or `.table-scroll`) ancestors; 0 leaks. Widest: `/captio-alternative/` 1113 px (scroll 763), `/vs/` 981 (631), `/blog/line-keep-alternative` 805 (455), `/vs/notion/` 518 (168), `/autopilot/` 10 × 460 (110).
- Above the fold at 390 px: H1 visible 16/16 (top 93–266 px); exactly 1 H1 per page; App Store badge above fold on all 7 conversion pages; hero images render (`naturalWidth` 109–264, 0 broken `<img>` across 16 pages / 71 images).
- Nav: hamburger 48×48 on 15/15 JA/EN pages, opens `#navLinks` (display flex, 10 rows, min row 48 px, `scrollWidth` stays 390); `aria-expanded` toggles; menu transparency in captures ignored per brief. Lang dropdown opens 10 links 110×44, right edge 72 px inside the viewport.
- Text: body 16 px on all pages; main `p/li` min 12.8–14 px (12 px only on `.zp__stamp` / badge labels); contrast body 15.6–18.2:1, H1 18.2:1, text CTAs 7.05–7.62:1, nav CTA 19.7:1 (`/autopilot/` "1.06" is a gradient-button false reading — rendered dark text on a cyan→blue gradient, legible in `autopilot-mobile-fold.png`).
- Theme: `color-scheme: dark`, body `rgb(7,11,20)` on all 32 loads; 0 U+FFFD; 0 page errors; desktop nav 1 row on 16/16.
- CLS API: 0 on 14/16 mobile loads, max 0.008 (`/autopilot/` desktop, nav); H1/hero/CTA boxes stable 0.5 s→3 s on 13/16 (exceptions = Finding 7).
- 07-07 items #4/#9/#17/#18, pill offset, 44 px toggle — held (table above).
- Traps confirmed for future audits (not defects): (a) headless full-page captures leave `content-visibility:auto` sections blank — `/` `.user-voice` shows a 2,727 px band of pure background at y 13,104–15,831 in `root-mobile-scrolled.png` yet has 1,034 JA chars in source; (b) `scroll-behavior: smooth` (`style.css:179`) makes scroll-then-shoot captures mid-animation (`en_captio-alternative-mobile-table.png` is black) — inject `scroll-behavior:auto` first; (c) a shared browser context carries `lang=en` from `/en/` into later dual-DOM pages; (d) `scrollWidth` is not an overflow signal on this site (see Method).

## 5. Owner-only actions

- Confirm the current App Store figure (4.2 / 25 per site-constants) before the 9 stale strings are re-synced — the store value is the owner's source of truth.
- Real-device (iOS/WebKit) re-check of the 16 pages is still owner-side: no WebKit here, and #814's bug class was Blink-invisible. The 0/16 overflow result is a Blink result.
- Read field CWV (CrUX / GSC) for `/` to confirm or dismiss the font-swap shift in Finding 7.
