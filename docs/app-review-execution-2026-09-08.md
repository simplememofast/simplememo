# 5.8.46の提出実行と採点範囲

オーナーが5.8.46と承認済み紹介画像を指定してApple申請を依頼し、Codexが既存の個別承認用ワークフローを実行した。

- [Submit to App Review run 34166798798](https://github.com/simplememofast/simplememo-ios/actions/runs/34166798798) は成功。2026-09-07 22:30:01 UTCに提出完了。
- 対象は5.8.46、build1179。VALID・APP_STORE_ELIGIBLEを確認して添付。
- 日英各3枚の紹介画像は承認済み出力とSHA-256が一致。Apple側の処理完了・元ファイルチェックサム・順序を検証後に提出。
- ASCを独立して読み、5.8.46(1179)のWAITING_FOR_REVIEWとAFTER_APPROVALを確認。
- 迅速審査フォームを提出後に送信し、Appleの受付確認画面を確認。審査通過や公開完了の証拠ではない。

ユーザーは、今回案内したWatchの通常送信、切断・再起動時の保持、再接続後の単一配送、上限で保持された履歴の長押し再送、再送後も次の入力が残ることを報告した。実行者はユーザーであり、AIによる実機操作とは扱わない。端末OSとビルド画面の独立確認、全リリースQA完了は主張しない。

`automation-coverage.json`の`ai_executes_gated`は、人の個別承認を必要としない機械ゲート経路を定義している。今回の成功は個別承認用`submit-review.yml`であり、`evaluateSubmission`が通った実績ではない。そのため分類と148/177のAI実行率は据え置く。台帳のhuman_onlyという分類だけでは表現できない「個別承認後にAIが実行した」事実を、この文書で明示する。分類の追加、分母変更、既存の定義変更は行わない。

次の条件は、既存ゲートが要求する同一コミットの実機確認記録・CI・TestFlight経過・審査枠等を満たした自律提出が実際に通ること。今回の個別承認実績でその条件を代替しない。
