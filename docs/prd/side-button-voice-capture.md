# サイドボタンからの音声Capture — PRD

作成日: 2026-09-05
状態: 実装中
対象リポジトリ: `simplememo-ios`

2026-09-19再照合。旧10/1提出日は実行日程として使用しない。

## 0. 一行定義

対応が確認できた端末のサイドボタンから既存音声入力を開き、共通Capture経路で保存する。

## 1. なぜこの機能か

`docs/VISION.md` §3.2と§6の画面を見ないCaptureと共通処理に対応する。
ローカル `claude/prd-side-button-voice-capture`（`adc5b20`）と
`claude/prd-routing-level1-2`（`6ddc66a`）に試作がある。同名ブランチのGitHub PRは9/19照合時0件。
iOS main `17dcad9` には通常のStartVoiceMemoIntentがあり、提案のside-button機能は未統合。

最新の統合先は[iOS PR594](https://github.com/simplememofast/simplememo-ios/pull/594)（Draft）、head `c52db6925a8c2b2081c1c638b0209d7b65d8bcd4`。main #604を統合し、通常音声の入力元観測と最終文字起こしの引継ぎを追加した。関連12 suite・292 native testsが成功（失敗/skip0）、277ソースの実行前後・コミット一致を確認。同headの必須CI5項目が成功。Cloud1740の全9ページで1,624試験×4モデル＝6,496実行を確認し、6,492成功・失敗0・実機専用の保護属性試験4skipである。skipは実機保護の合格に含めない。旧head `33e78f2` の移行検証はnative92成功/実機専用1skip、Cloud6,420成功/実機専用4skipとして分離する。既定OFF、追加配布なしを維持し、全入口・音声・保存先の同一配布版QAは未完了。

## 2. VISION §13 のチェック

1. **Capture Coverage / Zero-decision 率を上げるか**: 音声入力への到達を短くする。効果は実機と利用後に測る。
2. **整理するUIを増やしていないか**: 既存音声画面と短い案内を使用する。
3. **Destinationを所有しようとしていないか**: 既存の外部保存先へ送る。
4. **共通Capture Objectを通るか**: 既存MemoCaptureの受付IDと配送経路へ統合する。
5. **修正が学習資産になるか**: 聞き返しと保存先変更をRouting側の端末内記録に接続する。
6. **AIを前面に出していないか**: 「話す」「保存」の操作として案内する。

**Routing Level**: 入口のみ。保存先の選択はRouting側のLevel 1→2。

## 3. 受入条件

- [ ] 実装時点のApple公式仕様・SDK・entitlement・対応地域・アカウント条件を再照合し、根拠を残す。
- [ ] 既存のStartVoiceMemoIntentと共通Capture経路を再利用する。
- [ ] 許可済みの対応実機で割当・長押し・音声セッション開始を確認する。ウォーム起動5回の録音開始が各1秒以内。
- [ ] 返事は既定オンで初回にオフへ変更可能と案内する。成功文言は実際の保存結果に対応させる。
- [ ] 聞き返しは1回、無回答3秒で既定へ戻る。起動後3秒無音なら音声セッションを自動終了し、何も送信しない。
- [ ] マイク・音声権限の拒否と対応外端末を扱い、勝手に認証・既存保存先を変えない。
- [ ] オフライン待機、復帰配送、再試行の重複防止を同一配布版で検証する。
- [ ] 標準の静的検査・関連テスト・PR Checkを通す。実機未完了のまま提出しない。

## 4. UX

サイドボタンから既存音声入力へ進む。実保存を確認した結果だけ声で返す。
対応条件・割当手順は実機で確認した内容を案内する。

## 5. 多言語

| 要素 | JA | EN |
|---|---|---|
| 設定の案 | サイドボタンから話す | Talk from the side button |
| 返事の設定案 | 返事の音声をオフ | Mute spoken replies |

対応地域・OSの確定前に一般提供を約束しない。

## 6. 決めていないこと

Appleが用途を受け入れるかは審査結果で確認する。決めるのは: Apple。
次の提出版・日付は既存の実機・審査・公開ゲートで判断する。旧10/1提出の記録を自動実行しない。
現在の実機QA担当と操作を重複させず、旧版の証拠を新しい配布版へ転用しない。

## 7. 測り方

同じ版の起動→録音→受付→全宛先到達を測る。起動遅延、無音終了、失敗、聞き返し、母数を保持する。
採用率・D7は成熟した同一コホートで評価し、配布を効果と扱わない。
