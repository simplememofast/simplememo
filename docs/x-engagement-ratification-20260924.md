# Xの返信・引用RTの期限つき追認（2026-09-24）

2026-09-24、オーナーは既存の対外送信経路を1つずつ判断する規則（L4-03。CLAUDE.md の
「新しい種類の対外送信は、錠前ができるまで始めない」の節）に沿って、
Xの返信・引用RTを期限つきで追認した。このファイルはその判断の原文・範囲・期限・
失効時の扱いの記録で、権限表 `data/authority-matrix.json` の「SNS投稿（X・TikTok）」行の
`owner_delegation`（id: `x-engagement-ratify-20260924`）が根拠として指す。

## オーナーの判断（原文）

AIセッションが出した選択式の質問への、オーナーの回答（2026-09-24）。
**一次の記録はこの回答で、リポジトリの外にある。ここにあるのはAIによる転記。**

1. 前段の質問では「**期限つきで追認し台帳に載せる (Recommended)**」を選んだ。
2. 続く質問「X の返信・引用リポスト（x-engagement）を、どの範囲で期限つき追認しますか？」に
   「**観測どおり1日2回に絞る (Recommended)**」と回答した。その選択肢の説明（原文）:

   > 1日2回、各回は引用RT1件と返信3件まで、2026-10-24 に失効（失効したら人へ戻る）。
   > あなたがスキルに上限と失効日を書き足し、私が権限表の追認を draft PR で出します。

下の範囲・期限は、この回答をそのまま書き下したもの。数字も範囲も足していない。

## 範囲

| 項目 | 内容 |
| --- | --- |
| アカウント | 日本語の自社アカウントだけ |
| 回数 | 1日2回まで（オーナーが選んだ上限。選択肢の名前は「観測どおり1日2回に絞る」） |
| 1回あたり | 引用RT 1件・返信 3件まで |
| 対象外 | DM・広告・英語アカウント・TikTok |

## 期限と失効時の扱い

- 有効期間は 2026-09-24 から **2026-10-24T00:00:00+09:00 まで**。
  回答の原文は日付だけ（「2026-10-24 に失効」）で、失効の時刻をその日の 00:00 JST とするのは
  AI の解釈（狭い側に取った）。10-24 の日中も有効と読む必要があれば、人が決め直す。
- 失効したら人へ戻る（`expiry_behavior: new_decision_required`）。
  権限表の `ai_may` の項目は、失効後は根拠にしない。
- 再開・延長・範囲拡大は人が決める。止めるのはAIでもよい。

## 範囲に含めないもの（2026-09-25 追記）

- **いいね**と、**投稿へのブランド（製品説明）の織り込み**は、この追認の範囲に含めない。
  2026-09-25、AIセッションが「いいね・ブランドの織り込みを範囲に含めるかが未決定」と伝えたのに対し、
  オーナーは「**おまかせ**」と答えた。AI は広げる方向を選ばず、範囲に含めない側に決めた
  （AI が自分で決めてよいのは止める方向だけ、という既存の規則に合わせた）。
  範囲に加えるのは人の判断。

## 未了（オーナーの作業）

- **実行側（リポジトリの外）を、この追認の範囲と期限に合わせる。**オーナーの作業で、詳細はオーナーが非公開で持つ
  （対外送信の経路の棚卸しは公開リポジトリに置かない、という CLAUDE.md の規則に合わせた）。
- 数値はオーナーが選んだ1日2回のまま記録した。

## この記録の性質

- **AIがオーナーの判断を転記した「記録」である。**AIとオーナーの操作をGitHub上で識別できる
  状態になるまで、人の承認を機械が確かめたものとしては扱わない（`record_kind: record`）。
- 権限表の `self_repair.must_not`（権限表を自分で書き換えない）に対する明示の例外として足した。
  これを前例に、権限表を自分で書き換えない。
- **draft PR で出す。Ready にしてマージさせるのは人**（draft の間は auto-merge の対象外）。
  アクション台帳 `data/autopilot-actions.json` の `act-x-engagement-ratify-20260924` は
  人の担当（`force_owner: human`）の未完了として開けてあり、権限表が main に入ってから閉じる。
- 9月5日の投稿群の公開委任を広げたものではない。権限表の gate にある
  「返信・DM・広告はこの委任の対象に含めない」は9月5日の委任についての記述でそのまま残し、
  時限の例外（この追認）があることだけを1文足した。
- この領域の可逆性（権限表は `reversible: true`、`data/autonomy-score.json` の分類は R2）の
  食い違いは、この追認では解消しない。
- 追認は実行実績ではない。自動化率・自律スコアの台帳は変えていない。
- 権限表の検査（`scripts/check-authority.mjs`）は、期限つきの委任の形を日付に依らずに確かめる
  （発効・失効の日時が読めること、失効したら人へ戻す扱いであること、ai_may の項目がこの委任を指していること、
  消した委任を根拠にする項目が残らないこと）。
