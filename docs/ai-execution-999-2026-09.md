# AI実行率98%超と次回プレスリリース

2026-09-08開始。過去の目標変更を経て、**2026-09-12の最新ユーザー指示は98%超**。
現在の配信前条件は、同じ棚卸し定義・未丸めのAI実行率が **98%超** であること。
次回プレスリリースは2026-09-14〜20日（JST）の配信を目指す。未達なら配信・予約せず延期する。
ファイル名は既存の証拠参照を保つためそのままとする。

改善・検証・PR・マージ・本番デプロイ・今回1件の配信は包括承認済みで、個別確認を繰り返さない。
最新指示に従い、TestFlightはスコアが大きく上がる場合のみ配布する。既存配布版の実機証拠を再利用し、版・ビルド・SHAを照合する。
選択②Aに従い、本人の既存同意が確認できる利用者データのみを使う。
今回の数値目標の変更は、同意の代行、実行の先取り、価格上限や品質条件の変更を含まない。

## 最新目標と配信条件への反映

次回リリース `202609-autonomy-followup`（企業182412・下書き10）の配信操作はAIへ委任済み。
`scripts/press-release-next.mjs` の `OWNER_TARGET_AI_EXECUTION_RATE` を `0.98`、`target_comparison` を `strictly_greater` に固定し、
`evaluateDispatch` が実測、同じ棚卸し、鮮度、本文・宛先・日時・料金・未配信状態・品質を確認する。
生の比率で判定し、表示用の丸め値では判定しない。さらなる閾値変更は新しいユーザー指示が必要。

- **196/199 = 98.492462…%**：同意・未配信など3件が残っても数値条件を満たす。
- **195/199 = 97.989949…%**：表示は98.0%でも未達。
- **98/100 = 98%**：ちょうど98%は不可。
- **197/199 = 98.994974…%**：旧99%超条件では未達だったが、今回の98%超を満たす。

現在は実測158/179 = 88.268156…%で未達・未配信。実行者の分類は実行結果だけに基づく。
今回の権限・条件・優先順位の更新には実行率の加点をしない。

PR TIMESの下書き上書きは、最後の保存・読戻し確認から実測で **3パーセントポイント以上** 改善した時。
比較基準は155/178（表示87.1%）を保持する。目標98%超到達時の配信前最終同期は例外とし、
保存・読戻し成功前に基準を更新しない。

収集同意はApp Analyticsで各利用者が選ぶ共有オプトインである。
[Appleの説明](https://developer.apple.com/documentation/analytics-reports/app-store-opt-in)の対象も
共有を選んだ利用者であり、事業オーナーの承認を他の利用者の同意へ置き換えない。
既存の同意済みデータを利用しても、同意する行為そのものの実行者はAIにならない。
この業務の `human_consent` と未完了の分類は保持する。目標変更だけで全業務が実行可能になったとは扱わない。

**2026-09-12の配信前上限の照合:** 「収集同意」と「PR TIMESへの配信操作」をAI実行へ先取りせず、
ほかの残業務をすべてAIへ移せた場合の楽観上限は **197/199 = 98.994974874…%**。
今回の98%超を数値上は超えるが、物理業務や法的判断の実行可能性・期限内達成は保証しない。
`execution-priorities.mjs` はこの楽観上限と現在の分類による上限を別々に返す。
現在の分母179件を保つなら、残る実施中21件のうち18件の完遂で176/179 = 98.324022…%となる。
未着手を開始すると分母も増えるので、1件の実行効果を単純合算しない。

## 開始点と達成条件

開始時のmain `79f8f81d` は実行148 / 実施中177 = **83.615819%**。
総合自動化率148/199 = **74.371859%**、AI関与率160/177 = **90.395480%**、カバー率177/199 = **88.944724%**。
既存203タスク・意図的な除外4件の定義を維持する。2026-09-09夜の変更前は156/179 = **87.15083799%**。
2026-09-12の158/179から、既存業務をk件移管し、未着手をn件AIが実行した後の率は `(158+k+n)/(179+n)`。
残る実施中23件のうち配信以外の22件を移し、未着手20件も実行すれば配信前は198/199となる。
これは必要な算数であって、本人同意・物理行為などをAIへ移せるという証明ではない。
表示の丸め、業務の削除・分割、未実施行為の完了偽装はしない。公開済み9/3記事は配信時点の記録として凍結する。

完了には次のすべての現在の証拠が必要：

1. 同じ棚卸し定義・未丸めの実行率98%超。各移管行に対象・実行者・結果・日時・実体への参照。
2. 必要な改善がマージされ、実際の本番で動くこと。CI成功やコードの存在だけでは代用しない。
3. TestFlightはスコアが大きく上がる場合に限り、改善をまとめて実機で確認する。既存配布・他タスクと重複しない。
4. 実測98%超の確認後に新しいPR TIMES記事を配信し、公開URL・本文・日時・指標・配信状態を確認する。
5. 配信前に実測値へ原稿を同期し、そのスナップショットを凍結する。目標値を達成実績として書かない。

## 実行中の段取り

- `data/press-release-next.json` が今回1件の原稿、観測スナップショット、予定、下書きID、配信証跡を結ぶ。
- 配信の当初の暫定目安は **9/17（木）10:00 JST**。これは予約済みを意味しない。
  実測98%以下では予約を作らない。期間を過ぎた場合は達成後に日付と日付ゲートを更新する。
- PR TIMES企業ID 182412の管理画面に接続。9/8時点の一覧は配信済み6件で、次回下書き・予約は無かった。
- `scripts/press-release-next.mjs --refresh` で現在台帳から原稿を再生成。予約・配信後は書き換えを拒否する。
- `--check` は保存物の整合検査であり、公開許可ではない。
- 実際のPR TIMESプレビューから取得した材料を `--preflight --ui <非公開の観測JSON>` に渡す。
  実測98%超、24時間以内の同一台帳、5分以内の表示、同じ本文・企業・下書き・配信先・時刻、
  既存プラン内での配信、記事と結び付いた品質審査が揃わなければ停止する。
- マーケティングのD-SCOREはAI実行率とは別指標。続報であるだけの原稿を新規性満点にしない。
  現在の本文は数値と公開方法の基礎稿であり、次週までの新しい実行結果と素材・新規性の審査が残る。
- ハートビート `simplememo-ai-99` は既存IDを維持して現在のタスクへ移管し、98%超・2時間ごとの継続実行へ更新。
  予約が存在することと、自然起動して改善・配信を完了したことは別の証拠として扱う。

## 次の実務

**短時間で実行証拠が揃い、AI実行率が直接上がる既存業務の移管を優先する。**
開始時に `node scripts/execution-priorities.mjs` を実行し、直接効果と現在の待ちを照合する。
既存業務の移管と未着手業務の開始では分母への影響が違う。古い待ち・未評価の手間を
「簡単な加点」として繰り返さず、最新の評価・実行中の他タスクと突き合わせる。
実装済みで検証だけが残る業務、既存データで完了できる設計判断を先に進める。
計画や下書きだけを増やして加点しない。必要な未着手業務も進める。最初に他スレッドの実装・配布・
PRを確認する。現在の古い台帳には、包括委任より前の承認待ちや廃止済み実行元を前提にした
記述が残るため、現在の権限と実行環境を実物で照合する。

- DPA・データ利用・SLA・撤退計画：既存条項審査との重複を避け、サービス別の個人データ、
  適用契約、実際のプラン、再委託先、削除・退出方法を審査し、保留は理由付きで残す。
- D7/D28と課金・解約：既存の非公開コホート評価と購読状態を同一コホートへ結ぶ。
  検索データの28日待ち、状態在庫を解約イベントとみなす代用はしない。
- 課金導線・有料機能判断：実機能、既存の受入条件、利用者データに照らして判断し、
  対象コード・版・適用先を記録する。価格変更や新規支出をスコアのためだけに発生させない。
- 製品バグの再現テスト：問い合わせの実際の操作手順から生成し、対象製品で再現する。
  分類器のテスト生成は既に別行で計上済み。
- 配信・障害対応：App Review、段階公開、停止訓練、アプリ内障害案内を現状と突き合わせる。
  実機QA、審査、カナリアの母数・時間の経過を捏造しない。
- 全体予算・収益：全サービス費用・配賦・残高・同期間の収益を照合する。
  AI費用上限の1件を全体運営予算へ二重計上しない。

対象の51件は開始スナップショットの残業務。以後は最新の台帳と実行証拠を正本にする。
何も進まない時には「残タスクはほかにない？」を自問し、別の実装・観測経路へ進む。
ステータス報告だけを進捗にせず、実行中の待ちは実際のハンドルで確かめる。

## 実行記録

2026-09-08：今回の隔離worktreeとCodex/ブランチを作成。最新mainと公開済み9/3記事を照合。
配信前検査と原稿生成を実装。未達、数値の改変、古い観測、台帳変更、誤った企業・下書き、
本文の差異、配信済みの重複、期限外、新規料金、品質審査欠落、停止スイッチを実行検査する。
PR TIMES企業182412に下書きID10を実際に作成し、保存完了と本文の読戻しを確認。
同じ下書きを更新し、重複を作らない。この実装と下書き保存を「PR TIMESへの配信操作」の完了として加点しない。

同日：既存ChatOps PR #1140のCI失敗原因である公開ページの古い率を修正。
配信済み5.8.52の成功ワークフロー・配布先読戻しを根拠とする1件の反映で、追加配信はしていない。
Premiumの設計判断はiOS PR #441の全CI成功後にmainへ適用済み。
詳細は `docs/premium-benefit-execution-20260908.md`。この2件の反映後は150/177 = 84.745763%。
未移管は実施中27件、未着手22件。99.9%はまだ未達。

## 次回1配信の料金条件（2026-09-08確認）

認証済みPR TIMES企業182412の料金画面で、現在の従量課金プランを確認した。
[公式料金](https://prtimes.jp/price/)も基本料金は1件30,000円（税別）。
当初の検査が想定した追加課金0の配信枠は確認できなかった。
次回配信の包括承認に基づき、今回の下書き10だけを基本料金30,000円（税別）以内・
追加オプション0円・1配信に限定した。契約やプランの変更は含めない。
認証済み画面の請求予定金額は、その時点の集計であり、この下書き1件の見積もりへ代用しない。
個別の請求履歴は公開リポジトリへ転記しない。

配信前の非公開UI観測には、既存の本文・時刻の材料に加えて次を記録する。

- `pricing_observed_at`：配信前5分以内に実際の料金画面を確認した時刻。
- `pricing_company_id`：182412、`billing_plan`：従量課金プラン。
- `charge_scope`：this_release、`currency`：JPY、`charge_basis`：per_release_excluding_tax。
- `basic_charge_excl_tax_jpy`：今回1件の基本料金、`optional_charge_excl_tax_jpy`：0。
- `terms_changed`：false。旧無料枠用の `incremental_charge_jpy` は併記しない。

金額・税区分・口座・時刻が不明、上限超過、オプション追加、プラン変更や再配信は停止する。
これは配信完了の証拠ではなく、99%超・品質審査・本文照合の条件も保持する。


## 2026-09-12 目標98%超への更新

最新ユーザー指示に従い、配信判定・台帳の必要条件・権限表・優先度・継続予約を98%超へ同期する。
基準は実測158/179、総合79.4%・AI関与92.2%・カバー89.9%。加点は0件。
5.8.54/1342のApple実観測はWAITING_FOR_REVIEW、段階公開INACTIVEのため公開業務は待機を維持。
