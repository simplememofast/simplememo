# 本番Act停止・復元訓練（2026-09-12）

状態は完了。停止run34698150205・復元後run34698821083を照合し、実行率159/179（88.8%）、停止機構の本番観測4/11へ更新した。以下の実行前条件はそのまま保持する。
オーナーの今回の明示委任（必要な実行・デプロイを承認、都度確認不要）に基づき、
`ai90-act-stop-20260912` の停止・所有した状態だけの復元・観測をAIが担当する。
過去の未承認訓練を事後承認するものではない。

9月7日の主系移行後も日次Actと監視は`agents.act`を参照しているが、その本番停止は
未実証である。今回、既存の本番ワークフローが着手前に止まり、復元後に同じ検査を
通過することを確かめる。アプリの機能・資格情報・予算・全体停止は操作対象に含めない。

## 対象と実行条件

- 全体・各経路の停止がないこと、実際の事故や競合する変更がないことを直前に確認する。
- 変更するのは`agents.act`の停止状態・演習理由・準備時刻・実行者だけ。
  `policy.ai_may_resume`は変更しない。今回明示委任された演習の後片づけとして、
  演習IDを含む停止レコードが完全一致するときだけ元のレコードへ戻す。
- 復元直前にも実事故・新しい停止要求・エスカレーションを確認する。既存の`applyTrip`は
  既に停止中なら新しい停止要求でもレコードを更新しないため、完全一致だけでは
  演習中に実停止の必要が生じなかった証明にならない。運転台帳・監視Issue・新規run・
  事故記録を停止前後で照合し、実停止が必要、または確認できない場合は復元しない。
- 停止・復元はそれぞれ通常PRの検証済みSHAをmainへマージする。mainの更新・CI・
  配信状態を確認し、競合する本物の停止を上書きしない。復元差分も停止前に用意する。
- 停止中の既存`Autopilot Act`をmainに対して`dry_run=true`で起動する。
  これは本番の非公開検証実行であり、通常の`--apply`実行や日次PR発行は要求しない。
  停止の確認ステップの失敗と、その後の台帳準備・観測・記録処理のskippedを
  実run ID・取得済みコミット・ログで確認する。
- 停止を通過した場合は失敗として扱い、所有runをcancelする。cancelが先行処理を
  取り消すとは考えず、実際に進んだstepと発生済み変更を調べる。今回の入力は
  `dry_run=true`なので`--apply`とリモートPR発行は既存条件により抑止される。
- 復元後も同じ`dry_run=true`を使用し、停止ステップの成功と後続処理への到達を確認する。
  dry-runでもrunner内の台帳・公開ページ生成は書き換わり得るため、完全な読み取り専用とは
  呼ばない。任意のroutine observerも読取APIとrunner内の保存を行う。
  実公開・通常の書込処理の復旧は、これだけでは実証したとしない。

## 停止中の影響と復元

`agents.act`は指定runだけを止めるものではない。停止のmain反映から復元反映まで、
イベント起動を含む通常Act、同じ停止を参照するローカル監視も着手しない。
既に着手済みの処理を強制停止するものでもない。これを訓練の影響範囲として扱う。
既存owner-directが演習理由のオーナー向け運用通知とKV指紋更新を行う可能性がある。
通知経路を無効化せず、発生した場合は演習に付随する通知として記録する。

CIや同時変更があるため停止時間の上限は保証しない。停止前に復元内容と検証経路を確認し、
停止観測後は後片づけを最優先に進める。停止レコードが演習用の値と違えば自動で消さず、
変更元を調べて演習の状態だけを復元する。停止中の全runと新規PRを前後で照合する。

## 完了と加点

停止の本番観測、所有した停止の復元、復元後の検査通過、影響確認が揃って初めて
今回の実訓練を完了とする。機構`emergency-stop-agent`の本番観測と、業務200の
「本番で停止機構を実際に通す（訓練）」の実行証拠は区別して記録する。
業務199は機構の列挙・段階管理を担い、業務200の原定義に11/11の要件はない。
今回の成功が確認できた場合だけ業務200の移管を独立レビューする。
全11機構の実証、業務82のアプリKill、実障害の検知、将来の無条件の停止・解除を
主張しない。停止前の準備時点では、準備・承認・ローカル検査だけでは加点しないとした。

## 本番停止の観測と復元差分

2026-09-12T14:03:20Z開始の[本番run 34698150205](https://github.com/simplememofast/simplememo/actions/runs/34698150205)を、main `66f3d073df4674ed97142b0010f75265a2b3a178`、`dry_run=true`で実行した。
14:03:38Zに「緊急停止の確認」がfailureとなり、台帳準備・自己検査・観測・導出・
記録・公開ページ生成・PR作成の全ステップがskippedとなった。これは意図した停止の成功である。

復元差分は同演習ID・停止時刻・実行者・元の停止なし状態を照合して、
`agents.act`だけを演習前のレコードへ戻す。全体停止・別経路・解除ポリシーは変更しない。
これは復元PR作成時点の記録で、当時は復元後の本番検査通過と影響確認が未実施だったため加点していない。現在の完了結果は次節に記録する。

## 完了した本番観測

- 停止PR #1270のmain反映は14:00:09Z、復元PR #1271は14:16:26Z。停止は977秒。
- 復元PRのSEO Validation 34698266219は成功。Pages本番 `de53749b-1951-4797-be26-a4edd35532c9` は14:16:48Zに成功した。
- [復元後run 34698821083](https://github.com/simplememofast/simplememo/actions/runs/34698821083)はmain `aa8e860a8649727ddb5811c468e3c48ffa4acccb` を取得し、停止チェック・導出・台帳検査まで成功。入力は停止時と同じ `dry_run=true` で、リモートPR作成はskippedだった。
- 停止中のDecision Monitor 34698546697/34698556698は `emergency_stop: monitoring execution halted` で停止。復元後34698813042は同じ監視ステップを成功した。失敗を消したり、成功へ書き換えたりしない。
- 同時間帯の通常Act 34698546717/34698556715は取り込み不要の判定でactがskippedだった。日次scheduleの時刻は停止区間に含まれない。
- ローカル監視は14:13:12Zに既存GitHub監視の鮮度を確認して見送った。ネイティブ監視の実行は起動していない。
- 本番cron台帳では14:00:10Zのowner_directが `sent=1`、理由 `alerts=emergency_stop,owner_blocked` を記録した。訓練に伴う既存の通知経路の作動であり、メール到達を確認した記録ではない。通知・指紋の経路を無効化していない。
- Web/APIの既存Issue・事故台帳・新規PRを前後で照合し、演習と競合する実停止の理由は確認されなかった。進行中の他PR #1272/#1273と既存#1047はそのまま保持した。

[機械可読の実行記録](../data/production-stop-exercise-20260912.json)にrun、SHA、各step、通知、限定事項と原本ハッシュを保存する。既存業務200の名称と分母は維持し、訓練1業務をai_executes_gatedへ移す。機構の本番観測4/11と業務の実行率159/179は別の数であり、全機構の検証完了や本番アプリのKillを主張しない。今後の本物の停止の解除には引き続き別の条件が必要。
