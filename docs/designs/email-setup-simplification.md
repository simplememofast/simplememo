# 宛先設定の単純化候補

2026-09-07。AIによる候補化・採点の記録。製品変更の採用・出荷・効果判定ではない。

原資料の記録コミット: API `ae762b017c5724212620643461bef7ed94527f44`。

## 実測から候補へ

非公開APIリポジトリの `docs/cohort-language-auth-2026-09-07.md` は、2026-09-07 07:30 JST基準の本番コホートで、英語14 install中Apple provider_errorが6 install、初回起動から48h未満にエラー後のメモ送信が記録されたのは0 installと記録している。旧版の詳細エラーコードはunknownであり、Appleの障害や言語差を原因と断定できない。

観測窓は初回起動時刻 t0 から48h未満で、provider_error の受信より厳密に後の memo_send_success を数えている。エラー発生から丸48時間追跡した値ではない。

この観測は認証の摩擦を調査する根拠にはなるが、機能削除の根拠には不足する。そのためApple/Googleサインインの削除案は不採用とし、より小さい単純化を候補化する。

候補: 初期設定でメール手入力を見つけるための分岐を減らす。メール欄と次の操作を初期表示の主要導線として見せ、Apple/Googleは同じ画面の代替手段として残す。エラーが発生するまで手入力を案内しない構成への依存を減らす。メール検証自体は省略しない。

既存のiOS PR366・384が行ったOAuthエラー・キャンセル後の手入力案内とは別で、成功・失敗前の初期配置を対象とする。既存修正を新しい実績として数えない。

## 優先付け

`data/feature-backlog.json` の `email_setup_primary_manual` として既存の採点器に入力する。change_typeはsimplification。改善効果は未測定なのでevidence_strengthはhypothesisとし、実測された問題と改善仮説を分ける。expected_effect=5は既存採点尺度内の計画上の仮置きで、5ポイント改善や5%改善を意味しない。effort_days=2も設計・実装・検証の見積りで実績ではない。

採点器の結果は5×0.15÷2=0.375。「まず測る」へ送り、順位や今回の自動化目標を理由に直ちに製品へ適用しない。

## 実装前の確認と受入条件

- 現在の配布版の初期配置を基準として保存し、既に主要導線になっているなら変更せず候補を閉じる。
- 英語の小さい画面と最大文字サイズで、メール入力・検証コード送信・代替サインインが見つかることを確認する。
- Apple/Googleの選択、利用者自身によるキャンセル、メール所有確認、既存宛先の保持を維持する。
- 購入、価格、無料枠、個人情報の送信先、収集同意を変更しない。
- 成果判定は実際の初回メール送信までを対象とする。認証成功だけ、テスト送信、合成操作を成果に含めない。
- 小標本の観測なので改善率の確定値や言語間の因果比較を出さない。旧版・新版・内部端末を区別する。

VISION §13: Capture開始前の判断を減らす候補。整理画面やDestinationは増やさず、新しいCapture入口やRouting Levelも作らない。AIを前面に出さない。Routing修正学習は対象外。

この候補の登録は「機能の削除・単純化・高速化の候補化」に対する実行証拠。採用・出荷・改善サイクル完走の証拠には流用しない。
