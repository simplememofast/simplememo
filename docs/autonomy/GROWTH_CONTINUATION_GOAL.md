# Growth and autonomy continuation Goal

Continue the original `RESIDUAL_OPERATIONS_GOAL.md` and `AUTONOMOUS_COMPANY_GOAL.md`; retain their immutable baseline, pending natural-run evidence and safety boundaries. The owner has continued this work in an active native Goal. Private `GOAL_STATE.json` identifies its current receiving thread and the backed-up old-to-new mapping.

## Objective and current finding

Make the existing SEO/AIO PDCA consume verified actual measurements when choosing and verifying one eligible improvement. Preserve the existing native owner, collectors, report destinations, formal metrics, prospective value contracts and delivery gates. A Company report currently reads an older repository GSC snapshot even when a newer verified export has already been collected privately. Its opportunity list omits concrete rows from the GSC detectors.

## Checkpoints and verification

1. Reuse the existing fixed GSC export and canonical snapshot/CTR calculations. Validate its retained hash, query identity, provenance, dates, dimensions and full 28-day coverage. No duplicate API call, copied credentials or private Git data. Missing query/page or Google AI dimensions stay unavailable.
2. Pass the same data to the existing detectors and the Company decision input. Preserve CLI output parity, noise floors, disjoint comparison rules and active experiment protection. Evidence availability never grants publication permission. Include useful question-level evidence from the existing fixed visibility series without another paid probe.
3. Execute the integration on retained real data, verify exact aggregate/curve/detector parity and review actual eligible candidates through the existing gates. Retain a real decision, action, tests, delivery and follow-up evidence. Do not turn an observation-only receipt into a completed run.
4. Ship the reviewed change through exact-head SEO Validation and the existing Git/Pages route. Verify merge and actual served output. Re-run the integrated consumer from the merged code and recalculate formal metrics without modifying definitions or inventing a gain.
5. Continue residual natural-run verification through the existing daily owner. Seven distinct native days, original maturity and real weekly execution remain required; a manual Goal run cannot substitute.

## Stop, block, rollback and progress

Completion requires the above actual evidence and original applicable acceptance; code, a report, a timer or elapsed time is insufficient. Progress independent work while a source is unavailable. Preserve irreducible blockers privately and follow the native Goal repeated-blocker rule. Record verified/remaining/blocked/next, current commit/run and metrics in private `GOAL_STATE.json`. Roll back code through a revert PR and restore any target mapping only after reconciling pending delivery events. Never revive old owners or duplicate timers.
