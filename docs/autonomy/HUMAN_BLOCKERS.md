# Human blockers and source limits

Updated 2026-09-24. These do not suspend independent implementation or available-source operation.

| Scope blocked | Evidence / reason | Minimum human action | Permission | Work unlocked |
|---|---|---|---|---|
| Retained Cloud Scheduler definitions | Both existing CI and authenticated console confirm the API is disabled; this does not prove dormant definitions never existed | No authentication action pending. Do not enable the API for inventory: missed jobs can execute immediately when re-enabled | Any future activation needs its own side-effect and owner review | Metadata of dormant definitions, if any |
| Unattended BigQuery transfer metadata refresh | Actual encrypted CI inventory run 34746778461 authenticated successfully but received IAM_PERMISSION_DENIED for transfer metadata. Owner console inventory was inspected separately | No new grant requested; preserve the current analytics principal and use the scoped console observation | Existing metadata-read grants only | Machine-refreshable control-plane inventory; GSC/GA4 data reads already work |
| Daily autonomous shipping on route `actions` (Codex `obsidian` and GH `obsidian-autopilot` share one stop) — contained since 2026-09-21, no autonomous shipment 2026-09-20..24 | `scripts/autopilot-selfheal.mjs` counted the 2026-09-20 measurement-ownership gate rejection (`ap-20260920-actions-codex-01a0bb78…`, gate_code `measurement_scope_conflict`, self-reported `eligibility_verdict: blocked`) as a fourth unrepaired `no_artifact`, after three repairs with different causes (#1051, #1237, #1470). Gate rejections written by `scripts/lib/codex-run-intake.mjs` use `outcome: skipped_gate` without a failure class (as the 09-08 and 09-12 rows do); this row was written by hand as `no_artifact` (#1504). `policy.ai_may_resume` is false. An AI-side re-classification attempt on 2026-09-24 was refused by the session's permission check and was not retried. The escalation reached the owner only through the 07:00 JST report and the 3-day `owner_blocked` notice; the declared `gh_issue` route is not implemented | Owner decides L4-01 in `docs/zero-preneur-autonomy-roadmap-2026-09-24.md` §9 (recommended: A — the owner re-classifies that one row with the evidence recorded, then watches the next scheduled run) | Owner only (`ai_may_resume: false`) | Daily autonomous shipping; roadmap Ph0 exits ①②; every Ph2 sample |

The earlier assertion that authenticated console screens were required was premature. Existing CI credentials were reused first; their actual service-specific responses are retained. The owner then completed Google and OpenAI passkeys. The authenticated current ChatGPT active, paused and completed views were inspected, along with GCP scheduled queries, transfers, Dataform and the unfiltered orchestration table. There is no pending login request. The requested one-time authentication reminder was paused after fulfillment and read back from the native scheduler.

Private visible-UI evidence belongs in `discovery/browser-inventory.json`. It is account/project scoped, separate from API results, and expires for current-inventory purposes after seven days. Empty current views do not erase historical tasks, prove absence in other accounts, or override an API denial. See [Cloud Scheduler schedule behavior](https://docs.cloud.google.com/scheduler/docs/configuring/cron-job-schedules) before any future activation.

Other limits currently need investigation or observation, not a new user permission:

- Documented native weekly visibility and funnel owners remain unmatched in the inspected scheduler. Native IDs can be reused by unrelated reminders; match purpose and recurring trigger as well as ID. Resolve ownership before adding a replacement reservation.
- ASC Analytics is partial and crash reports are `not_received`. Provider absence is not zero crashes or zero traffic. Existing scheduled collectors remain canonical.
- Apple Search Ads has historical account/sign-in evidence but no verified configured data connection. The user restricted this integration to existing usage, so do not provision a new one.
- The worker `vfu_regular` has recurring counted errors, while `last_error` is null. A current 24-hour aggregate confirms the counter but not the root cause. Investigate the existing dispatch/suppression/token path; do not replay customer sends or broadly change credentials.
- Existing Obsidian recovery verification requires its next natural scheduled run. An installation Goal/manual run is not a substitute for that other task's acceptance condition.
- Historical manual decisions, verification and reporting are not all separately instrumented. Keep unknown values null; do not infer H0 from a successful process or an empty legacy interventions array.

Private per-source evidence and any later blockers belong in `~/.config/simplememo/company-os/`. Public documentation must never contain credentials or raw customer/account records.
